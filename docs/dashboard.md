# Dashboard

How `apps/web` (React Router 8 SSR on Cloudflare Workers) is built, authenticates against the API, and what it ships in which phase. The dashboard consumes the same public API as everyone else; there are no private endpoints (the framework test from [overview.md](overview.md)). Everything visual follows [design.md](design.md).

## Packages

| Package | Role |
|---|---|
| `@buzzkit/ui` (`packages/ui`) | The design system, ported 1:1 from feedbase: shadcn (Base UI style) + Tailwind v4 tokens, Open Runde, Central Icons pipeline (`generate:icons` scans the repo for `name='Icon…'` literals). Chat-specific pieces were left behind; everything else is identical |
| `@buzzkit/eden` (`packages/eden`) | Eden Treaty over `@buzzkit/api/contract` (`apps/api/src/modules/contract.ts`: the v1 router without runtime adapters). Unwraps the `{ success, data, error, metadata }` envelope so callers get `data` or a normalized `{ status, value: { code, message, param } }` error. Types are inferred from the API's emitted declarations (`bun run types:emit` in `apps/api` → `.types/`, git-ignored; turbo runs it before `dev` / `build` / `check-types`) |
| `@buzzkit/web` (`apps/web`) | The dashboard. `app/lib/api.server.ts` wraps the Eden client per call (bearer token + `buzzkit-workspace` / `buzzkit-tenant` headers), maps 401 to a sign-out, and derives every entity type (`Workspace`, `Credential`, `Tenant`, …) from calls, never by hand |

## The server-side proxy pattern

The API authenticates `/v1/*` with `Authorization: Bearer <token>` only ([authentication.md](authentication.md)), and BetterAuth is mounted on the API at `/v1/auth` (`authHandler`). The dashboard adds nothing in between:

1. The browser signs in **directly against the API** with the BetterAuth client (`lib/auth.client.ts`, `baseURL: API_URL`, `basePath: '/v1/auth'`): `signIn.email`, `signUp.email`, `signIn.social({ provider: 'github' })`. The API answers with its own httpOnly session cookie (`buzzkit.session_token`, `__Secure-` prefixed in production). CORS on the API allows `DASHBOARD_URL` with credentials.
2. That cookie reaches the dashboard because dashboard and API are **same-site**: on localhost, cookies ignore the port; deployed, both live under one parent domain and the API sets the cookie on it (BetterAuth's `crossSubDomainCookies`, the domain derived from `BETTER_AUTH_URL` and `DASHBOARD_URL` by `@buzzkit/auth/cookies`, nothing to configure). That also puts the cookie on the root domain, so a landing page there can tell a signed-in visitor from a new one. A self-hoster puts API and dashboard under one domain for the same reason.
3. Route **loaders** read the cookie (`lib/session.server.ts`: `requireSession`, `readSessionToken`) and forward its value, BetterAuth's signed session token, as the bearer header on server-side `/v1/*` calls via `lib/api.server.ts`. The Worker validates nothing itself: the API verifies the token on every call, and a forged or expired cookie is a 401 that expires it. Where the Worker does need BetterAuth's answer it uses BetterAuth's client server-side with the cookie forwarded: `requireAnonymous` on the auth pages asks `getSession`, `signOut` goes through `signOut`. The browser never sees the token: the cookie is httpOnly and nothing is stored client-side.
4. Signing out is an intent (`sign-out`) on the action of whichever route shows the control (the workspace layout for the account menu, the invite page for its "Sign out"): `signOut` in `lib/session.server.ts` calls `/v1/auth/sign-out` with the bearer token and forwards BetterAuth's own `Set-Cookie` headers to the browser, so BetterAuth decides how its cookie is expired. Only an API 401 (a session BetterAuth no longer knows, so sign-out cannot clear it) falls back to `signedOutRedirect`, which expires the cookie by the names and domain `@buzzkit/auth/cookies` exports.

`BETTER_AUTH_URL` is the API origin plus `/v1/auth` (BetterAuth routes by that path, so `authHandler` re-adds it to the request Elysia's `mount` stripped), and GitHub's callback URL is `<BETTER_AUTH_URL>/callback/github`, i.e. `http://localhost:8790/v1/auth/callback/github` locally. The dashboard shows the GitHub button when it has the OAuth app's public `GITHUB_CLIENT_ID`; the API holds the id and the secret and runs the flow.

`/login` and `/signup` share one pathless layout (`routes/(auth)/layout.tsx`, a pathless group in Next.js' bracket notation) that renders **one persistent form** (`components/auth/form.tsx`, `AuthForm`): the routes only contribute `handle.auth` (mode, title, description, footer link) and loader data (`apiUrl`, providers, redirect, error), and render nothing themselves. Switching between the two keeps Email, Password and the submit button exactly where they are; the Name field is the only thing that moves, rising out from behind Email (its grid row unfolding from `0fr` to `1fr` while the field inside keeps its natural height, translate up from 24px, scale from 0.92, fade in, 0.4s bounce-0 spring) and folding back the same way; the "Must be at least 8 characters" hint under Password does the mirror image, unfolding downward from behind the input. The fields below Name sit on their own `bg-card` layer so Name really comes from behind them. `/invite/:token` renders its own static card; workspace creation (`/onboarding`) uses the onboarding layout. Login, signup and the invite card all sit inside `AuthPage` (`components/auth/page.tsx`), the signed-out frame that paints the marketing hero's brand wash and drifting notification cards around the card ([design.md](design.md) → Auth pages).

Field-level problems (a bad email, a short or breached password) show under the field; anything that is not about one field (wrong credentials, the API unreachable, a GitHub round trip that failed) is a `toast.error`, never an inline banner. Toasts are always top-centered.

Passwords must be at least 8 characters (`minPasswordLength`) and are checked against Have I Been Pwned's breached-password corpus on sign-up (BetterAuth's `haveIBeenPwned` plugin, k-anonymity: only a hash prefix leaves the server); a compromised password comes back as `PASSWORD_COMPROMISED` and the form says so under the field.

The only cookie the dashboard writes itself is `buzzkit.workspace`, the last visited workspace slug, unsigned because it is not security-relevant. Only `/`-relative `redirect` values are honored (`safeRedirect`).

There are no action-only routes: a mutation belongs to the action of the route that renders its control, selected by an `intent` field (the workspace layout handles `profile`, `sign-out` and `create-workspace`; the invite page handles `sign-out`). Every action body lives under **`lib/actions/`** (`onboarding`, `workspace` (profile, sign-out, create-workspace, settings incl. delete, and the shared form-to-workspace mapping), `invite`, `connect`, `channels`, `members`, `tenants`, `keys`, `topics`, `subscribers`, `messages`; `context.server.ts` has `beginAction`, the env/token/form preamble they all share); a route file only does `export const action = workspaceAction`. `lib/api.server.ts` is the typed API client those actions and the loaders call.

Two rules fall out of `api.server.ts`:

- **Any API 401 expires the cookie and redirects to `/login`.** Loaders and actions never handle expired sessions themselves.
- Envelope failures throw `ApiError(status, code, message, param, details)`; actions map codes to field messages (`conflict` on `POST /workspaces` → "This slug is already taken"). `param` lets the onboarding guide attach a provider rejection to the field that caused it.
- `unwrap` JSON-round-trips every payload: Treaty revives ISO strings into `Date`s at runtime but the derived types say `string`; the round-trip makes the runtime shape match so loader data serializes predictably.

### Tenant selection

Every data-plane call carries `buzzkit-tenant`. The workspace layout resolves **which tenant** once per request (`resolveTenant` in `lib/session.server.ts`: `?tenant=<slug>` on any route wins, then the `buzzkit.tenants` cookie, a per-workspace map of the last choice, then `default`), validates it against `GET /v1/tenants`, falls back to the default tenant for an unknown slug, writes the cookie when the choice changed, and hands the `Tenant` down through the outlet context; every data loader and action reads the same resolver (`beginAction` returns `tenant`), so nothing hardcodes `default` any more except the first-run onboarding, which is default-only by design. The `?tenant=` form is the shareable one: a colleague opening it lands on that tenant and stays there. The layout's loader follows the same rule as every page: it awaits only local work (the session, the tenant choice, the cookies it writes) and returns the chrome (workspace, workspace list, profile, tenants, the resolved tenant, connected channels) as one promise made of five API calls in a single round (the credentials are fetched for the requested slug alongside the tenant list, and only refetched when that slug turned out unknown and the default tenant took over). The component never suspends: it settles the promise in an effect and renders from what it knows. The sidebar is always the real sidebar, because its navigation needs only the slug; the workspace switcher and the account row come from this workspace's chrome when it is known (`recallPage('layout:<slug>')`, remembered in an effect so the server store stays empty), otherwise from the last chrome seen in the session (`layout:last`: the workspace list and the profile are the same for every workspace, so a switch shows the target's name and avatar at once and only the tenant section waits), and on a cold first visit the two slots are placeholders of the exact same size (`SwitcherPlaceholder`, `AccountPlaceholder` in `components/layout/sidebar.tsx`; the avatar is real, seeded by the slug). The content card renders the `Outlet` as soon as a cached or live chrome exists (the pages need `tenant`, `role` and `connected` from the outlet context, so they cannot mount earlier) and the page's own skeleton until then: every route exports React Router's `handle` inline as `export const handle: PageHandle = { skeleton: (…) }` (`components/loading/handle.ts` only names the shape; the handle is React Router's per-route metadata, read from the deepest match with `useMatches()`), and that skeleton is built from the very components the page renders while its own data is pending. Each route keeps a `<Page>Header` (its `PageHeader` from `components/layout/page-header.tsx` with the real title and description, plus everything static: the time range, the filter bar, a lookup field, a CTA that every role gets) and a `<Page>Skeleton` (the `TableSkeleton` with its real columns, the Overview tiles and chart cards, a detail page's frame), and both the handle and the `Deferred` fallback compose the same two, so the layout's first paint and the page's cold load are the same pixels and mounting the page over the handle changes nothing on screen. Only what is actually loading is a skeleton: a button that depends on the role is the real button, `disabled` until the role is known and dropped once the role says no (the same rule as filter controls on a cold load), a text the data decides is a line of the same height, anything the role decides follows the role the layout already knows (`hooks/use-known-role.tsx`: the layout wraps the handle skeleton in `KnownRoleProvider` with the role from the session's last workspace list, or from the `buzzkit.roles` cookie, an httpOnly per-workspace hint the layout loader writes the first time it sees a workspace, awaiting the workspace list once for it, and keeps for an hour, cleared on sign-out; `useCanManage` resolves a header's `canManage` from it, so neither a switch nor a reload of a workspace where you are a member flashes an owner's buttons or cards), an input is a plain block of the input's size (never a framed empty input), a chart is its box. Nothing in the dashboard shows a spinner in place of a page. Detail pages do the same with a `<Page>Frame` (the back button, the header and the tab pills, from the URL) around a `<Page>Pending` body; routes with an editor (New workflow, the workflow test page) export a handle of their frame alone. The tenant banner above the content card follows the requested tenant slug, not the chrome: it appears on the first frame with the tenant's name as a small placeholder until the chrome names it, so the content never jumps when the chrome arrives. A 404 or 403 from the API resolves the promise to a failure that renders the not-found or no-access page inside the shell; a fresh workspace without a credential renders a `Navigate` to its onboarding once the chrome is known. Its `shouldRevalidate` re-runs it only when the slug changes or when a navigation *asks* for a different tenant: a `?tenant=` param that differs from the current URL's. A navigation that merely drops the param (every sidebar link after a switch) does not, because the cookie already carries the choice; the switcher links the current tenant without the param for the same reason. The outlet context carries `tenantSlug`, the requested slug straight from the layout loader, next to the `tenant` the chrome resolved; `Deferred` keys its stale-page cache and its Suspense boundary by that slug and the pathname, so a tenant switch remounts the boundary and shows the page's skeleton (or the rows that tenant showed last time) instead of holding the previous tenant's rows on screen until the new ones arrive.

**Tenants are not projects.** A workspace with one tenant never shows any of this: no switcher section, no suffix, no banner; the simple-app case never learns tenants exist, exactly like the API. With more than one tenant, the workspace switcher's trigger reads `Repro · Acme Corp` (tenant in `fg-2`, only while a non-default tenant is selected; on the default tenant the trigger is just the workspace name) and its menu gains a **Tenants** group below the workspaces (building glyph, check on the active one, "Manage tenants" into Settings → Tenants); picking one navigates to the current path with `?tenant=`. Viewing anything but the default tenant is deliberately not seamless: on the tenant-scoped pages (Overview, Workflows, Subscribers, Segments, Topics, Messages, Events, Settings → Channels; never on workspace-level pages such as API keys, General, Members, Tenants or the Audit log) an amber strip sits above the content panel, on the subtle background, the same 32px row at the same 12px top as the workspace switcher ("Viewing tenant Acme Corp. Its subscribers, topics, messages and credentials are isolated from every other tenant." with a "Back to default" action), because a tenant view is for inspecting or debugging one customer's data, not a second home. The onboarding redirect applies to the default tenant only: a fresh non-default tenant shows the Overview with its setup checklist and Settings → Channels with Connect buttons. Workspace-level pages (General, Members, API keys, Events) do not change with the tenant.

## Layout: sidebar + content card

**Connected channels are decided once.** The workspace layout loader already lists the tenant's credentials (to send a fresh workspace to onboarding); it derives `connected` from them with `lib/channels.ts` (`connectedChannels`, catalog order) and hands it down through the outlet context. Every channel picker, filter and column reads that list: the Messages channel filter (hidden when only one channel is connected), the send dialog's Channel select, the topics dialog's channel rows and the topics table's per-channel columns. No page lists credentials for itself, and nothing in the dashboard offers push to a workspace that never connected a push provider.

Every signed-in page lives in `routes/[slug]/layout.tsx`: the page background is `background-subtle`, a **240px sidebar** on the left, and the route's content inside a raised `bg-card` panel (`rounded-2xl`, `shadow-sm`, 8px gutter, `px-8.5 py-7.5`) that scrolls on its own. The panel is a **flex column** (`flex flex-col overflow-y-auto`): as a row it would stretch the route to its own height and the overflow would never become scrollable. The page itself never scrolls, vertically or horizontally: tall pages scroll inside the panel, and wide content (tables) scrolls inside its own `overflow-x-auto` container. There is no top bar; the pill tabs from feedbase are gone. Pages fill the panel: no `mx-auto`, no max width. Each page opens with a header row, `justify-between items-center`: title (`text-2xl`) and one-line description top-left, the page's actions top-right and vertically centered on the title block; detail pages put the avatar or tile beside the title and their destructive action in that same top-right slot. Page descriptions say what the page is for and stop: "Manage workspace API keys.", "Manage notification topics." No explanations, no reassurance, no feature copy.

The sidebar (`components/layout/sidebar.tsx`) is data-driven from `components/layout/navigation.ts`, the single source of the information architecture:

- **Top**: the workspace switcher as a full-width row. The first row of the nav itself is **Search** (the magnifying glass and a `⌘K` / `Ctrl K` chip, resolved from the platform after hydration), which opens the command menu: it is a row of the nav's `HighlightList`, so the one sliding highlight visits it under the pointer like any other row and glides back to the active page when the pointer leaves or the row is clicked; it is never the resting position, because it is not a page. **A clicked row releases focus.** Every nav row (Search, the page links, the expandable groups) blurs itself on click, so a pointer click never leaves the row focused and a later keypress (Escape on the command menu, ⌘K, an arrow) cannot make the browser draw a focus ring on it; Tab still focuses rows and shows the `focus-visible` ring, so keyboard users lose nothing. The command menu inherits this: opened from the Search row, there is no focused origin to return to, so closing it leaves focus on the page. **Bottom**: the account menu as a full-width row (avatar, name, grabber chevron). Between them the nav scrolls. Under `lg` the search row's job moves to a magnifying-glass icon button in the `MobileBar`, next to the sidebar button.
- **Sections** with quiet `text-xs fg-2` labels: Overview (unlabelled) · **Messaging** (Messages with their deliveries, Workflows expandable into Catalog and Runs, Events expandable into Catalog and Stream: what the tenant sends and reacts to, first) · **Audience** (Subscribers, Segments, Topics) · **Developers** (API keys, Webhooks, Sources) · **Workspace** (Settings, expandable: General, Channels, Tenants, Workflows, Members, Audit log, Billing `Soon`). Channels (the provider connections) are configuration and live under Settings, not in the main nav; a broadcast is a message to a segment sent from the Messages page and templates belong inside workflows, so neither gets a tab; no section ever holds a single item (Engage held only Workflows once Campaigns went; Messages and Events joined it as Messaging); deliveries are reached through a message, not a tab of their own.
- **Every row is a ghost button in disguise**: items, the workspace switcher and the account row share the ghost `Button`'s metrics (`h-8`, `rounded-xl`, `px-2.5`, superellipse corners; avatar rows keep `pl-1.25` so their text lines up with item labels) and its hover surface (`bg-a2/70`, press scale 0.985, gentler than a button because the row is wide). Sub-items use the `sm` size (`h-7.5`, `rounded-[10px]`). **One sliding highlight** (`useAnimatedIndicator`) across the whole nav, sub-items included; it rests on the active page and follows the pointer, exactly like menus and the onboarding rows. Icons are 18px and lift to 85% under the highlight or when active.
- **Expandable items** (`children`) are buttons with a rotating chevron, never a destination: the highlight visits them only under the pointer (to open or close), the resting highlight always sits on the active sub-item. Their sub-items unfold and fold (height + opacity, 0.3s bounce-0 spring opening, 0.2s closing, `AnimatePresence` so closing animates too) behind a left rail, and the chevron rotates in 150ms and open automatically when one of them is the current page. `Soon` items render dimmed with a badge and do not link.
- **Every page in the sidebar resolves.** Pages that are not built yet fall through to `routes/[slug]/planned/index.tsx` (the `*` child of `/:slug`), which looks the path up in `navigation.ts` and renders an `EmptyState` naming the phase (`planned`). A path that is not in the navigation, or is marked `soon`, is a 404. When a real page ships, its route is added above the catch-all and the `planned` field is removed.

The settings pages no longer carry their own sub-navigation: Settings' sub-items are the sidebar's.

### Command menu

`⌘K` (`Ctrl K`) anywhere under `/:slug` opens the command menu (`components/command/menu.tsx`, rendered once by the workspace layout on `CommandDialog` from the design system). It is the keyboard way through the whole dashboard, and it knows where you are:

- **On this page** comes first, and it fills itself. Every `Button` in the design system asks for an `ActionRegistryProvider` above it (`@buzzkit/ui/components/action-registry`, the same optional `register(action) => unregister` contract as the filter registry) and, when one exists and the button is enabled, registers its text as the label, its `icon`, and an `activate` that clicks the real element, so a `render={<Link />}` navigates and an `onClick` opens its dialog exactly as a pointer would. `PageHeader` wraps its `actions` slot in that provider, wired to `registerAction` in `hooks/use-commands.ts`, so a page's header CTA (Send test message, Create key, Create topic, Import subscribers, New segment, Create workflow, Create endpoint, Create source, Invite member, Create tenant, Add secret) is a command the moment it renders, under the very words on the button, and disappears with the button when the role says no or the page hides it. Nothing in a list route registers anything. Commands, header actions and facets all sit behind one `createRegistry<T>()` in `hooks/use-commands.ts` (an owner-keyed map, one publish, one `useSyncExternalStore` hook per registry). The manual form remains only for actions that do not sit in a page header: `useRegisterCommands([...])`, which re-registers only when the commands' ids, labels, hints, icons, keywords or destinations change and keeps the latest `run` closures behind a ref, (or `<PageCommands commands={[...]} />` from inside a `Deferred` render prop) with a label, an optional `hint`, `icon` (a string literal, so the icon generator sees it), `keywords`, and either `to` (a path, or a URL with `external: true`) or `run`. Today that is the workflow page (Test workflow, Edit details), a message (Copy message id and, while it can still be stopped, Cancel message) and a subscriber (Copy external id).
- **Filters** are picked up automatically from whatever `FilterSelect` and `FilterRange` the page renders. The design system's filter controls register themselves as facets (`FilterRegistryProvider` in `@buzzkit/ui/components/filter-bar`, a generic `register(facet) => unregister` context the workspace layout wires to `registerFacet` in `hooks/use-commands.ts`): the facet's label, its flattened options as plain strings, the current value and the same `onValueChange` the control calls. No page declares anything. A facet also knows the card it sits in: `Card` keeps the text of its `CardTitle` in a context (`useCardTitle`), so a filter bar inside the Activity card announces `scope: 'Activity'` while a bar in the page header has none. A page-level bar (Messages, the audit log) is the group "Filters"; a bar inside a card is always its card's group, "Activity filters", "Deliveries filters", whose items read "Filter activity by event", and Clear filters splits the same way ("Clear activity filters"). A `PillTabs` strip that filters rather than navigates joins by taking a `label` ("Status" on a message's deliveries and a workflow's runs, "Outcome" on a source's deliveries, "Time" on the event charts): it registers the same facet shape with `clearable: false`, because its neutral pill ("All", the default range) is one of its options rather than an absence. Tab strips that switch views (a workflow's Steps / Code / Versions / Runs) take no label and stay out of the menu. With an empty query the menu shows one "Filter by status", "Filter by workflow", "Filter by time" item per facet (the current choice as the hint) that opens the facet as a page inside the menu, listing "Any …" and every option with the current one checked; while typing, the options themselves match directly ("scheduled", "last 7 days", a workflow's name) under a "Filter by …" group. Picking closes the menu and sets the filter exactly as the bar would, through `useFilters`, so the URL, the pending spinner and the rows behave identically. While any *clearable* facet has a value (a `FilterRange` with `allowAny={false}`, the Overview's period, registers `clearable: false` and offers no "Any" option in its page), a **Clear filters** item leads the Filters group (and matches while typing), closing the menu and clearing every active facet through its own callback, one after another in the same tick; `useFilters` composes each pick on the heading it just wrote (read from the store at call time, not from the render), so successive picks stack instead of the last one winning. A `FilterRange`'s presets register; the custom calendar range stays in the bar.
- **Live results** come from the server, and only for things that exist. From two characters on, the menu debounces 200 ms and loads `/:slug/search?q=` through a fetcher: a loader-only resource route (`routes/[slug]/search/index.ts`, registered under the workspace layout, so the tenant cookie and session apply) that runs the API searches in one round (subscribers by `search=`, messages by `q=`, event names, workflows, segments, webhooks and sources filtered by name, slug, description or url; up to five per kind, every call tolerant of an API error) and returns `{ q, results }` typed by `SearchResult` in `lib/command.ts`. When the query is a prefixed id or an event name (`parseJump`), the route verifies it with the entity's `get*` call instead and returns that single hit, so a pasted `msg_…` that does not exist offers nothing rather than a 404. The menu shows results grouped by kind (Subscribers, Events, Workflows, Segments, Messages, Runs, Webhooks, Sources) as force-mounted items, because a hit found through an email or a description need not contain the typed text; it only renders results whose echoed `q` still matches the input, shows a spinner in place of the `esc` chip while a lookup is in flight, and when results land it moves the highlight to the first of them, so Enter opens what you were looking for rather than whatever was highlighted before.
- **Recent** (only with an empty query) lists the last pages visited in this workspace, entity pages named by their id or slug (`describePath`), kept per workspace in `localStorage` under `buzzkit.recent:<slug>`; the current page is left out.
- **The sidebar, as groups** comes next, in sidebar order and with the sidebar's own structure (`listSections` over `navigation.ts`, `Soon` pages skipped): **Switch** first (Switch workspace and Switch tenant, each only when there is more than one), then **Workspace** (Overview, or Quickstart with a filled rocket while the tenant has never sent a message), **Messaging** (Messages), **Workflows** (Catalog, Runs, Secrets), **Events** (Catalog, Stream), **Audience** (Subscribers, Segments, Topics), **Developers** (API keys, Webhooks, Sources) and **Settings** (General, Channels, Tenants, Members, Audit log). An expandable sidebar page is a group of its own and its children keep their own labels, so nothing needs a secondary text to say where it lives; the group heading says it. Items are valued as `section label` so the two Catalogs stay distinct and typing "workflows" finds the Workflows group. Synonyms per page (`push`, `apns`, `team`, …) are `keywords`. **Matching is by word, not by scattered letters** (`scoreCommand` in `lib/command.ts`, passed to cmdk as `filter`): every typed word has to be the start of the label, the start of a word inside it, or a substring, or the same against a keyword at a discount; an exact label outranks a prefix, a prefix outranks a keyword hit. cmdk's default fuzzy scorer was dropped because "tenant" matched Topics, Segments and Audit log through their letters and buried the tenant items. Most destinations also carry a **chord**: with nothing focused, press `G` then the key shown on the item (`G O` Overview, `G M` Messages, `G W` Workflows, `G R` Runs, `G E` Events, `G L` Stream, `G S` Subscribers, `G G` Segments, `G T` Topics, `G K` API keys, `G H` Webhooks, `G I` Sources, `G ,` Settings, `G C` Channels, `G P` Members, `G A` Audit log); the second key has one second to arrive, and chords stay quiet inside inputs, with a modifier held, or while any dialog is open.
- **Workspace** offers Switch workspace and Switch tenant when there is more than one, each a page inside the menu (a crumb in the input, `Backspace` on an empty query returns to the root); typing a workspace or tenant name from the root finds it directly.
- **Search** (any non-empty query) is contextual and never dead-ends, so the menu has no empty state. The page's own search field comes first: a `FilterSearch` given `onValueChange` announces itself through the same filter registry (`kind: 'search'`, its placeholder as the label), so on Messages the first fallback is “Search messages for …” and on the audit log “Search the audit log for …”, each typing the query into that field exactly as the keyboard would. Below it the cross-page lookups follow: Look up subscriber “…” (`/subscribers?q=`, which on the Subscribers page is the page's own search and therefore first) and Search the audit log for “…” (`/settings/audit-log?q=`, omitted on the audit log itself where the field already covers it). Live results carry the query and their label and hint as keywords, so a hit found through an email still scores and sorts with the matches rather than falling to the bottom.
- **Help** links the documentation, the API reference and the quick start (new tab); **Account** copies the page link and signs out (the layout action's `sign-out` intent).

**Navigation from the menu is as instant as a sidebar click**, and for the same reason: prefetch. Sidebar links carry `prefetch='intent'`, so hovering one has already discovered the route, loaded its module and started its loader before the click. cmdk items are not links, so the menu derives a map from each navigational item's value to its path from the same data the items render from (the `values` builders name every item exactly once, for the item and for the map) and renders one hidden `<Link prefetch='render'>` to whatever item is currently highlighted (`value` / `onValueChange` on the root): arrowing or hovering onto Messages, a workspace, a search hit or a recent page discovers the route and prefetches its module and data right then, so Enter commits with everything in hand and the page paints its handle skeleton (or its remembered rows) at once. That is also what makes a workspace switch from the menu match the switcher's speed.

Closing resets the menu completely: the query, the sub-page and the highlighted item all clear, so it always reopens at the root with the first item highlighted rather than on whatever was picked last. Entering a sub-page (Switch workspace, Switch tenant, a facet) sets the highlight to that page's first item explicitly and puts focus back in the input (a mouse click had left it on the vanished item), and Backspace back to the root re-highlights the item that opened the page, because cmdk keeps a controlled value that no longer names anything and would otherwise show no selection; new search results move it to the first result the same way. Every keystroke also drops the highlight, so cmdk re-selects the first visible item for the new query and scrolls it into view; without that, an item that happened to keep matching while you deleted characters stayed selected and left the list scrolled to the middle once the query was empty. There is always a highlighted item. Selecting closes the menu first and then acts. The dialog has no trigger element, so Base UI's default focus restore would land on the first focusable thing in the page (the workspace switcher, with a ring); the menu instead remembers what was focused when it opened and hands that back through `finalFocus`, or `false` when it was the page body, so dismissing with Escape leaves focus where it was. A `run` command runs on the next tick and also declines the restore, so a dialog it opens keeps focus; navigation goes through `navigate()` so the loaders and `Deferred` behave exactly as a sidebar click. The menu holds no data of its own: workspaces and tenants come from the layout's chrome, page actions from the registry, and everything else is derived from the URL.

**Under `lg` (1024px) the same layout folds into a phone layout, in the layout route alone.** The sidebar is `hidden lg:flex`; in its place the `main` column opens with a 40px bar (`MobileBar` in `layout.tsx`): a sidebar icon button, the workspace switcher and the account avatar. The button opens the very same `Sidebar` inside a left `Sheet` (`w-72`, `background-subtle`, no close button: the backdrop, Escape and any navigation close it, the layout resets `navigationOpen` on every pathname change). The content panel keeps its card, gutter and corner radius but drops its padding to `px-4 pt-5` (`sm:px-6 sm:pt-6`), and its inner column is a plain block under `lg` (`lg:flex lg:flex-1 lg:flex-col`): that one class switch is what turns every page from "chrome fixed, table scrolls in place" into one document that scrolls inside the panel, because the route roots' `min-h-0 flex-1` chain only binds when their parent is a flex column. Two-column detail pages already stack (`lg:flex-row`), and with the chain unbound their `overflow-y-auto` columns stop scrolling on their own, so nothing nests a scroll region inside a scroll region on a phone. The tenant banner keeps its name and the `Back to default` action and hides its sentence under `md`. Nothing else in the dashboard checks the viewport in JavaScript; every other responsive decision is a Tailwind variant in the design system or the page (`docs/design.md` §13 "Small screens").

## Route map

Workspaces live at the dashboard root, `/:slug`, which is why every top-level dashboard route name is a reserved workspace slug in the API (`RESERVED_SLUGS`).

| Route | Purpose |
|---|---|
**Loading model.** Every route loader returns promises rather than awaiting the API, so navigation lands immediately; the page renders its header and static controls at once and passes the promise to `Deferred`, which shows the rows this path resolved to last time for this tenant while it refreshes and the page's skeleton only on a cold load. The same header and skeleton are the route's `handle.skeleton`, which the workspace layout paints before the page can mount, so a workspace or tenant switch never shows anything the page itself would not. The workspace layout revalidates only on a workspace change, a tenant change or a non-GET submission, and sidebar links prefetch on intent.

**Live model.** The dashboard is live by default, not static-until-reloaded. The workspace layout calls `useLive()` (`hooks/use-live.ts`) once for whichever page is mounted, and that hook revalidates **the route's own loader** every five seconds, so liveness is a property of the shell rather than something each page remembers to ask for. Subscribers, Messages and their deliveries, Runs, Workflows and their live run counts, the Events catalog and each event, Sources and their deliveries, Webhooks and their ledgers, API keys' last-used times, the audit log and the Overview all fill in on their own. A page opts out through the `handle` it already exports (`{ skeleton, live: false }`), which is the whole vocabulary: Topics, Segments and their editors, New workflow, the workflow test page, workflow secrets and the settings forms are configuration only this dashboard changes, and the event Stream keeps its finer-grained direct Tinybird query, which a loader refresh would reset.

Because the refresh is a loader revalidation rather than a client fetch, one rule covers everything: the page keeps its rows (`Deferred` renders the resolved data straight through the refresh, so **no skeleton ever appears on a refresh** — only on a cold load), React Router applies the update in a transition so nothing flashes and open dialogs keep focus and typed text, `navigation.state` never moves so filter and search spinners stay quiet, and scroll position is left alone. The layout's `shouldRevalidate` keeps the chrome out of it, so a refresh re-runs the leaf loader and nothing else. It pauses while the tab is hidden and refreshes on return, and stands down while a navigation, a revalidation or any fetcher is in flight, since revalidating aborts in-flight fetchers; `idle` is one of the effect's dependencies, so the timer re-arms only after the last refresh settles and refreshes never overlap or queue.

| `/` | Pure redirect: no session → `/login` · no workspace → `/onboarding` · else last-visited (or first) workspace. In production the root of buzzkit.dev is the marketing site, so the app's entry is `/dashboard` (same loader); every post-login and post-onboarding redirect targets `/dashboard` |
| `/dashboard` | The dashboard's entry on the shared buzzkit.dev hostname: the same redirect as `/`. The marketing worker owns the root and its listed paths; the web worker takes `buzzkit.dev/*`, and every marketing path segment is a reserved workspace slug, enforced by `apps/api/test/utils/reservedSlugs.test.ts` |
| `/login`, `/signup` | Auth forms; visiting with a session bounces to `/` |
| `/onboarding` | Create the **first** workspace, step one of the onboarding (progress lines, step 1 active); with any workspace already, loader and action both redirect to `/` (further workspaces come from the switcher dialog, intent `create-workspace` on the workspace layout action); the API creates the `default` tenant in the same transaction, then the dashboard continues into `/:slug/onboarding`. With workspaces in hand the loader redirects to `/` and creation happens in the workspace switcher's dialog, whose fetcher posts to this route's action |
| `/invite/:token` | Public invite preview; accept with a matching signed-in email |
| `/:slug/onboarding/*` | The setup flow (below): one route with a splat (`/`, `/:channel`, `/:channel/:provider`), its own focused shell outside the workspace nav. One route rather than nested ones so the card can keep a fixed header and footer while only the content animates between steps |
| `/:slug` | Workspace layout (workspace + workspace list + profile in parallel) and the floating shell: switcher left, `Overview · Settings` pill nav centered, account menu right |
| `/:slug` (index) | Overview: a `FilterRange` for the period, six stat tiles (subscribers, messages, delivered, failed, events, runs), a Scheduled strip while sends are waiting, the Deliveries, Events and Runs charts, the Recent messages, New subscribers, Top events and Active workflows mini tables, and a **No channel connected** banner above the tiles when the tenant has no credential, all from `GET /v1/stats`, the two lists and the credential list on every load |
| `/:slug/settings` | General: workspace name and slug, and for owners the Delete workspace card |
| `/:slug/settings/channels` | Channels: the selected tenant's providers per channel, connect / validate / replace / remove; the send policy; identity verification (require it, reveal, copy and rotate the identity secret) |
| `/:slug/settings/members` | Members and pending invites: roles, remove, invite by email, resend, revoke |
| `/:slug/settings/tenants` | Tenants: create, edit name and slug, delete |
| `/ui`, `/design.md` | Design-system preview and the design doc (development only; both 404 when `ENVIRONMENT` is not `development`) |
| `/admin`, `/admin/workspaces` | Admins only (anyone else is redirected to `/dashboard`): its own sidebar chrome, built from the workspace sidebar's `SidebarNavigation` over `ADMIN_NAVIGATION`, with a `Back to dashboard` link and the account menu; new admin pages are new entries there. Overview (`/admin`): a per-minute throughput strip (deliveries, messages, events, runs over `GET /v1/admin/rates`, polled every ten seconds through the resource route `/admin/rates`), then platform-wide subscribers, messages, deliveries, events and runs over `GET /v1/admin/stats`, with leaderboards for most messages, most events, fastest growing and newest workspaces, plus top events, sharing the workspace Overview's charts (`components/overview/charts.tsx`); no live refresh. Workspaces (`/admin/workspaces`): every workspace on the deployment, searchable by name, slug or member email, each row opening the workspace. Reached from the account menu's Admin item, shown when the layout's admin probe (`GET /v1/admin/workspaces`) answered 200. Opening a workspace from here lands on `/:slug` as an owner: an amber "Support view" strip above the top bar, the workspace pinned in the switcher with a `Support` label, every workspace listed under ⌘K's Switch workspace, and no cookie remembering it as the last workspace ([admin.md](admin.md)) |

### Redirect matrix

| Route | No cookie | Dead token | Authed, 0 workspaces | Authed, N workspaces |
|---|---|---|---|---|
| `/` | → /login | 401 → clear cookie → /login | → /onboarding | → /:lastWorkspace (else first) |
| `/login`, `/signup` | render | → / → clear → /login (no loop) | → / → /onboarding | → / → /:slug |
| `/onboarding` | → /login | 401 → clear → /login | render | → / (creation lives in the switcher dialog) |
| `/:slug` (and everything under it) | → /login | 401 → clear → /login | → /onboarding | no connected provider yet → `/:slug/onboarding`; else render |
| `/:slug/subscribers` | → /login | 401 → clear → /login | → /onboarding | table (newest first, 50 per page, `?cursor` / `?trail` for paging, `?q=<externalId>` looks one up) or the identify empty state |
| `/:slug/subscribers/:externalId` | → /login | 401 → clear → /login | → /onboarding | detail: attributes, subscriptions (mute / remove), topic × channel preferences, workflow runs (every run of the subscriber, newest first, rows linking to the run), delete; unknown id → 404 |
| `/:slug/messages` | → /login | 401 → clear → /login | → /onboarding | paginated table of sent messages with status and funnel, "Send test push" dialog |
| `/:slug/messages/:id` | → /login | 401 → clear → /login | → /onboarding | overview (with the schedule and Cancel message while it can still be stopped), delivery funnel, payload, filterable deliveries table, rows expanding into the attempt ledger |
| `/:slug/campaigns` | → /login | 401 → clear → /login | → /onboarding | every campaign with its topic, audience estimate and status; Create campaign for admins |
| `/:slug/campaigns/new` | → /login | 401 → clear → /login | → /onboarding | details (name, slug, description), audience (topic, optional segment) and the notification (title, body, image, deep link); Create campaign makes a draft |
| `/:slug/campaigns/:campaignSlug` | → /login | 401 → clear → /login | → /onboarding | header with Launch or Cancel and the actions menu (send a test, delete), the delivery funnel and engagement counts, and the messages the campaign has produced. Launch opens a dialog showing the rendered notification, the topic and the audience estimate, and asks for the campaign name to be typed when the estimate is 1000 or more |
| `/:slug/workflows` | → /login | 401 → clear → /login | → /onboarding | every workflow with its trigger summary, status and live run counts (running / sleeping / waiting); Create workflow for admins |
| `/:slug/workflows/new` | → /login | 401 → clear → /login | → /onboarding | details (name, slug, description), the definition editor (JSON with the lint's problems inline) and a live preview of the trigger and steps; Create workflow makes a draft |
| `/:slug/workflows/:workflowSlug` | → /login | 401 → clear → /login | → /onboarding | header with Pause / Publish and the actions menu (test, edit details, delete); one card with `?tab=` pill tabs: **Steps** (the definition as a flow: the trigger or schedule card, cancel rules, concurrency and the default timezone, every step kind including fetch, set and after background, with the number of runs at each step), **Schedule** (only for a schedule workflow: what it fires on, the timezone, the audience segment, the next fire, and the recent fires with the runs each started, from `GET …/schedule`), **Code** (the spec editor, Save as draft → the next version, **Test** → the dry-run dialog for the latest version), **Versions** (each version opens its flow with **Test this version**), **Runs** (`?status=` filter, cursor paged, rows link to the run). The Test dialog takes a subscriber external id or made-up attributes, the trigger event's data, a start instant and an assumption per `waitFor` (arrives or not, with data) and `fetch` (status and reply), posts to `POST …/test` and draws the path on the flow with the trace of what every step would do; nothing is sent |
| `/:slug/workflows/:workflowSlug/test` | → /login | 401 → clear → /login | → /onboarding | dry run of a version (a version picker in the header, `?version=`): subscriber (a combobox over `GET /v1/subscribers?search=` through the route's own loader `?q=`, or made-up attributes), event data, the clock the run starts at, assumptions for every `waitFor` and `fetch` (fields come from the version's steps); the flow is always drawn, `POST …/test` then replays the path step by step (250 ms each) and lists what every step would do. Reached from the actions menu and the version dialog |
| `/:slug/runs` | → /login | 401 → clear → /login | → /onboarding | every run across workflows, newest first, `?status=` and `?workflow=` filters, cursor paged; rows link to the run and to its workflow |
| `/:slug/runs/:runId` | → /login | 401 → clear → /login | → /onboarding | the flow of the version this run started with (never the latest definition) with the run's path drawn in the accent color (taken lanes, reached steps, the current step marked with its state), the run's timeline (every `$run.*` event, sends linking to their message), and the overview (workflow, subscriber, status, current step, version, times) beside them |
| `/:slug/segments` | → /login | 401 → clear → /login | → /onboarding | every segment with its conditions summarised, version and last update |
| `/:slug/segments/new` | → /login | 401 → clear → /login | → /onboarding | the builder: conditions (builder rows or JSON), details, the live count and member sample |
| `/:slug/segments/:segmentSlug` | → /login | 401 → clear → /login | → /onboarding | the same builder over a saved segment, Save, Send to segment, delete |
| `/:slug/topics` | → /login | 401 → clear → /login | → /onboarding | paginated table with per-channel resolved defaults, create / edit dialog, delete |
| `/:slug/events` | → /login | 401 → clear → /login | → /onboarding | the catalog: the volume chart (`?range=`) over the pinned table of every event name |
| `/:slug/events/stream` | → /login | 401 → clear → /login | → /onboarding | the stream: every event newest first, always live, filtered by name and source, paged |
| `/:slug/events/:name` | → /login | 401 → clear → /login | → /onboarding | one event name: tiles, volume, recent payloads, the inferred fields |
| `/:slug/settings/audit-log` | → /login | 401 → clear → /login | → /onboarding | the workspace audit log: filterable (`?event=`, `?actor=`, `?range=`, `?q=`) paginated table, rows expand in place into request details and the event data |
| `/:slug/keys` | → /login | 401 → clear → /login | → /onboarding | workspace / tenant / client keys in three cards, create dialog (kind, tenant, preset or custom scopes), shown-once secret with a prefilled first call, rename, rotate (same shown-once dialog for the new secret), revoke |
| `/:slug/workflows/secrets` | → /login | 401 → clear → /login | → /onboarding | Workflows → Secrets (sidebar, next to Catalog and Runs): the tenant's write-only workflow secrets (`secrets` on `PATCH /v1/tenants/:slug`, names only from `GET`), add and remove, values never shown |
| `/:slug/webhooks` | → /login | 401 → clear → /login | → /onboarding | endpoints with health, the create dialog (URL validated as you type, description, tenant when there are several, every event or a picked set plus your own names) ending on the signing secret |
| `/:slug/webhooks/:id` | → /login | 401 → clear → /login | → /onboarding | one endpoint: the delivery ledger (status filter, expandable attempts and payload, resend), overview, secret reveal and rotate, enable / disable / delete |
| `/:slug/sources` | → /login | 401 → clear → /login | → /onboarding | the tenant's sources with provider, status and last delivery; Add source (name, provider preset, optional secret) ending on the ingest URL to paste into the provider |
| `/:slug/sources/:id` | → /login | 401 → clear → /login | → /onboarding | one source: the mapping editor with a sample payload and the event it becomes, the delivery ledger (outcome, result, expandable payload, Use as sample), overview with the endpoint URL, edit name and secret, pause / resume, delete |
| `/:slug/settings/channels`, `/members`, `/tenants` | → /login | 401 → clear → /login | → /onboarding | settings pages; members without `invites:read` see an empty Invites card instead of a 403 |
| `/:slug/*` (planned page) | → /login | 401 → clear → /login | → /onboarding | `EmptyState` naming the phase; unknown path → 404 |
| `/:slug`, `/:slug/onboarding` | → /login | 401 → clear → /login | 404 from the API | shell; API 404 → `NotFoundPage`, 403 → `NoAccessPage` |

`lastWorkspace` is written into the session cookie by the `/:slug` layout loader and on workspace creation, so `/` returns you to where you left off.

## Onboarding

**Onboarding is mandatory.** A workspace with no credential (no connected provider) has nothing to show, so `routes/[slug]/layout.tsx` redirects every page under `/:slug` to `/:slug/onboarding` until the first credential exists; there is no separate "completed" flag to keep in sync, the connection is the completion. It is **first setup only**: once the workspace has a credential, `/:slug/onboarding` (any path under it) redirects to `/:slug`, exactly like `/onboarding` redirects an account that already has a workspace; further channels and providers are managed in Settings → Channels. **Every step inside the flow is a client-side render, not a fetch.** The three things the loader reads — the workspace, the profile and the credentials — are the same on every step, so `shouldRevalidate` returns false for any navigation that stays on the same `slug`, and the loader runs exactly once per entry into onboarding. Which channel, provider and sub-step to show is derived in the component from `params['*']` and the `step` search param, never from loader data, because data that would go stale the moment revalidation is off has no business being in the loader. Moving between channels, providers, guide sub-steps and back went from ~1.5s of awaited API calls per step to under 100ms with no request at all. The same rule keeps the Connected screen alive after a successful connect (`actionResult.ok`), which would otherwise be redirected away by the loader's own `credentials.length > 0` guard. The 404 for an unknown channel or provider stays in the loader (`assertOnboardingPath`), where a typed URL still lands as a document request. The product promise is "sign up, upload your provider key, send". The onboarding is one **centered card** (the `Card` anatomy: header, content, footer strip) with six thin progress lines above it (`Workspace → Channel → Provider → Connect → Import → Integrate`). The only other thing on the page is the account row across the top — `Signed in as <email>` at the left edge, a ghost `Sign out` at the right — because onboarding is the one signed-in surface with no sidebar, and someone who lands here as the wrong account otherwise has no way out. It spans the page at every width (the email truncates rather than wrapping), so the two ends stay where the eye expects them instead of moving to the middle on a phone. The flow is still not skippable from inside (the dashboard stays reachable by URL).

- **Progress** (`components/onboarding/progress.tsx`): five `h-1` tracks in `bg-3` with a `fg-4` fill that animates its width on a 0.5s bounce-0 spring. Done steps are full, the current step carries a small fill that grows with sub-progress (the Connect line fills as the guide advances), upcoming steps are empty. No numbers, no labels, no icons; the element is a `progressbar` with `aria-valuetext` for screen readers.
- **Workspace** (`/onboarding`): name + slug, shown in the onboarding card with the progress lines (step 1 of 6). The default tenant is created automatically; the user never sees it.
- **Channel** (`/:slug/onboarding`, "Connect a channel"): `ChoiceRow`s (icon tile, name, one-line description, chevron) for every channel from `catalog.ts`: Push notifications and Email are available; SMS and Web push render dimmed with a `Soon` badge. Channel rows never show `Connected` (the badge belongs to the provider rows, where the connection actually is); a channel with a credential simply stays clickable. Rows share one sliding highlight (`useAnimatedIndicator`, the same mechanic as menus and the settings nav): a `bg-a2` pill glides to the hovered row, fades out when the pointer leaves, and scales to `0.9925` on press; the `IconTile` brings its own `bg-4/70` hairline ring; rows draw no background of their own, never move, and cast no shadow.
- **Provider** (`/:slug/onboarding/:channel`, "Choose a push provider"): the same rows per provider, platform as small badges (Apple `iOS`, Firebase Cloud Messaging `Android`; Resend, with Postmark and SendGrid listed as `Soon`). Every channel shows this step, even with one live provider, so the choice is explicit. Footer: `Back`.
- **Connect** (`/:slug/onboarding/:channel/:provider`): the provider guide, **one sub-step at a time**.
- **Signing out** is a plain `Form method='post'` with `intent=sign-out` posting to whichever onboarding route renders it, so both `onboardingAction` and `connectProviderAction` answer it (`signOut` clears the cookies and redirects to `/login`). The email comes from the loader that each route already runs, and `OnboardingLayout` takes it as a required `email` prop so a new onboarding surface cannot forget the escape hatch.
- **The card has three slots** (`OnboardingLayout`): a header whose text switches instantly while its height eases on the same 400ms curve (a `SizeAnimator` around it, so a description that wraps to one more line never snaps the card), a content area wrapped in `StepTransition` (`components/onboarding/transition.tsx`), and a footer anchored below it. Every view of the funnel (channels, providers, each guide sub-step, the connected state) produces `{ title, description, content, footer }` (`OnboardingSlots`); the route component picks the slots for the current URL and guide state and hands them to the shell. The transition is keyed by view + sub-step and its direction comes from a numeric position (`channels 0 < providers 100 < guide 200+n < connected 900`), stored in state the moment the key changes, so Back always slides back. Because the footer lives outside the animated region, it simply rides the card's height animation (`SizeAnimator`, 400ms to match the spring) instead of jumping. The guide's state lives in `useProviderGuide`, keyed by provider; its Next button submits the content's form by `form` id since they sit in different slots. The Apple guide follows the real 2026 portal flow: Keys → Register a New Key (tick APNs, Configure) → Configure Key (Environment `Sandbox & Production`, Key Restriction `Team Scoped`, which Apple locks after saving) → Register and Download → Key ID from View Key Details → Team ID from the portal header (`Team name - TEAMID`, top right) → bundle ID from Identifiers.

### The provider guide

The guide is the heart of the onboarding. Inside the card: the sub-step's title and one-line description as the card header (there is no static guide title; the step is the header, and it switches instantly while the content below animates), the illustration (a `bg-2` panel whose inner box keeps the mock's own `16/10` ratio, so the padding reads the same on every side), an optional outbound link (`Open Keys`, `Open the Firebase console`, …), and the field(s) this sub-step produces. The footer carries `Back`, `n of N`, and `Next` (enabled once the sub-step's fields are valid; Enter in a field does the same) or, on the last sub-step, the connect action. Sub-steps swap with a 0.4s bounce-0 spring, directional (`AnimatePresence mode='popLayout'` so the entering step starts while the leaving one is still on its way out): the leaving step slides a full width away, scales to 0.85 and blurs to 2px (no fade, so it has to leave completely); the entering one comes from a full width on the other side, scaling up from 0.85 while its 4px blur resolves to zero. Row pages (channels, providers) never scale: whatever is on the other side, a row page enters and leaves with the slide and a 1 to 1.5px blur only. The 0.85 scale and heavier blur belong to preview pages (guide steps, connected) in both directions, so going back from a guide to the provider list means the guide scales out while the list slides in flat. The card edge clips the slide horizontally and the animator's own box clips vertically via a `clip-path` inset that is open on the sides, so a step that shrinks the card never spills under the footer inside a `SizeAnimator` that bleeds past the card padding so shadows and the slide never clip; the centered `n of N` counter rolls with NumberFlow on the same 400ms curve and the primary label swaps from `Next` to the connect label with `TextSwap` (NumberFlow-style vertical roll with masked edges, the button width springs to fit).

- Each guide (`components/onboarding/guides/apns.tsx`, `fcm.tsx`, `resend.tsx`) is an ordered list of sub-steps: title, description, link, note, **fields**, illustration. Fields sit inside the step that produces them: the `.p8` drop zone under "Download the .p8 file", the Key ID input under "Copy the Key ID", the service-account JSON drop under "Upload the JSON file". There is no environment picker: the API detects it from the key. While a file-field sub-step is showing, **the whole page is the drop target**: dragging a file anywhere raises a full-screen overlay (portal, blurred backdrop, dashed panel) that already judges the type mid-drag from the MIME info the browser exposes (unknown types pass, clearly wrong ones turn the panel red and say what is expected); on drop the file name and type are checked against the field's `accept` list, a wrong file is rejected inline under the drop zone without being read, and a right one is read in the browser (`file.text()`) and submitted as a hidden field; the APNs `.p8` is checked for a PKCS #8 header and the Firebase JSON for `project_id` / `client_email` / `private_key` before anything is sent.
- **A refresh keeps your place.** The sub-step lives in the URL (`?step=n`, written with a replace navigation that `shouldRevalidate` exempts from re-running the loader, so even the server render lands on the right step) and the non-secret answers (Key ID, Team ID, bundle ID, which fields were touched) live in `sessionStorage` per workspace and provider, cleared on success. **Secrets are never stored in the browser**: the `.p8`, the service-account JSON and the Resend key have to be dropped or pasted again after a reload, and the guide steps back to that field if a later step was open.
- **The connect form always carries every field of the guide**, including fields on sub-steps that were skipped because their value was derived (the Key ID read from `AuthKey_<ID>.p8`); only the visible steps decide what the user sees, never what gets sent. When the API points an error at a field on a skipped sub-step, that sub-step is un-skipped and shown with the error; an error that points at no field at all is shown under the last sub-step, so a rejection is never silent. The primary button never changes its `type`: it is always a plain button that calls `requestSubmit()` on the last sub-step (a button that flips to `type='submit'` inside the click that reaches the last step would be submitted by the browser's default action on that very click). The derived map is persisted alongside the public values, so the step list and `?step` numbering survive a reload.
- **Validation waits for you to finish.** A field's error appears when you leave it or press Enter, never while you type; editing a field that already shows an error hides the error again until the next blur or Enter. Enter on a valid field is the same as Next, and on the last sub-step the same as the connect action, so a one-field guide like Resend connects from the keyboard.
- **Fields that can be derived are.** A file field may `derive` other values from the file (the APNs `.p8` is named `AuthKey_<KEYID>.p8`, so dropping it fills the Key ID), and a step marked `skipWhenDerived` drops out of the sequence while every field it owns came from a derivation, so the Key ID step only appears when Apple's file name doesn't carry it. Example ids in placeholders and illustrations are invented but shaped like the real thing (ten uppercase alphanumerics).
- **Connect** posts one form (every field travels as a hidden input, so earlier sub-steps stay part of the submission) to the route action, which builds the `CredentialUpload` (`guides/upload.ts`) and calls `POST /v1/credentials` on the default tenant. The API validates with a real provider call and, for APNs, probes Sandbox and Production itself (one `.p8` covers both when the key was configured for both, so the guide asks for no environment) and answers with one credential per accepted environment. All `active` renders the connected state (environments listed), `unvalidated` (provider unreachable, which is what local `wrangler dev` on macOS produces for APNs) renders an amber "saved, not verified" state with `Validate again` (`POST /v1/credentials/:id/validate` for each), and a provider rejection (400) surfaces inline; when the API names a `param` the guide jumps back to the sub-step that owns that field.
- **Connected state**: same card, header with the provider glyph, a row list (status badge, environment, Team ID / Key ID / bundle ID, or project + service account), footer with `Add another channel` (only while other channels are unconnected) and `Continue`.
- **Import** (`/:slug/onboarding/:channel/:provider` after Continue, the fifth progress line): first "Migrating from another provider?", two `ChoiceRow`s (`Import subscribers`, which opens the form as the next view, and `Start fresh`, which goes on to Integrate) with `Back`; then "Bring your subscribers", the `ImportForm` described under Subscribers → Import inline in the card with `Back`, moving on to Integrate when the import is handed to the background runner. Positions 950 and 1000 in the transition order, so Back always slides back; the choice view is a row page, the form a preview page. `ChoiceRow` takes `onClick` instead of `to` for the row that switches views.
- **Integrate** (the sixth and last progress line, position 1100): "Integrate BuzzKit", the `IntegratePanel` (`components/integrate/panel.tsx`, shared with the Overview's quick start): one full-width **Copy agent prompt** button (the prompt is never previewed; the label rolls to "Copied" through `TextSwap` for 1.5s) that puts "Add BuzzKit notifications to this project. Install the skill from https://buzzkit.dev/skill.md and follow it. The client key is bk_pk_…" (plus the API origin when it is not the hosted one) on the clipboard, which an agent turns into the whole integration because the skill at that URL carries every step, and under it the default tenant's live **client key** in a `CodeBlock` whose hint is the one sentence "Ships inside your app and reaches the client API only." (where backend keys come from is the quick start's own workspace-key card, not this hint). The loader reads the key from the workspace's client keys (the one created with the workspace, matched on the default tenant's id) the same way the Subscribers empty state does, so the prompt is complete without a visit to API keys; a revoked key leaves the prompt pointing at the API keys page. Footer: `Back` (to the migrate choice) and **Open dashboard**, a link to `/:slug`, which lands on the Overview's quick start.

Illustrations are composed from `components/onboarding/illustration/` primitives (`Browser`, `Sidebar`, `Page`, `PageTitle`, `MockButton`, `MockInput`, `MockCheckbox`, `MockRow`, `MockTabs`, `MockDialog`, `Spot`, `Callout`) and follow the structure and copy of the real Apple Developer, Firebase and Resend screens; the single sky `Spot` ring marks what to click or copy. They are tokens-only so they read as part of the product, sized for the card (the mock sidebar hides under `sm`), and meant to be tightened against real screenshots of those dashboards.

## Subscribers

The first data page, and the pattern every list + detail page follows.

- **List** (`routes/[slug]/subscribers/index.tsx`): page header with the lookup field in the top-right slot: no button, it navigates to `?q=` 300ms after the last keystroke with the field's `loading` spinner on until the page has loaded, and clearing it returns to the list (the API has no search, and subscribers are addressed by *your* id, so lookup is an exact `GET /v1/subscribers/:externalId` and either shows that one row or says it does not exist), then a paged `Table` (the shared stack: `readPage` / `paginate` / `TablePagination`, 50 per page): **Subscriber** (avatar initials, `name` or `externalId` with a `Verified` badge, the id or email underneath, the whole cell links to the detail), **Country** (flag from `public/flags` + `Intl.DisplayNames` name, from the system attribute `$country`, `Unknown` when the device never identified), **Channels** (one badge per way to reach them, from the list's `channels` and `platforms`: `iOS` blue, `Android` purple, `Email` amber, `None`; green stays reserved for `Verified`), **Subscribed** (`Time`), **Last seen** (`TimeAgo` over the list's `lastSeenAt`, `Never`). System attributes (`$country`, `$city`, `$region`, `$timezone`, `$language`) are stamped by the API on device requests, never by the dashboard. A tenant with no subscribers gets an `EmptyState` inside the same card as the table (the one empty-state pattern every list page follows: Messages, Topics, Events, API keys, Subscribers) whose identify `curl` carries the *selected tenant's* live client key (`bk_pk_…`, auto-created with every workspace and tenant, public and therefore readable from the key list), falling back to a workspace-key placeholder for a tenant whose client key was revoked. Messages cannot do the same: `POST /v1/messages` needs a secret workspace or tenant key, which the API shows once and never again, so that snippet keeps a placeholder. The page shows the selected tenant's providers.
- **Import** (`components/subscribers/import.tsx`, `ImportForm` and `ImportDialog`): the migration path from another provider ([api/imports.md](api/imports.md)), reached from three places and never from a header button, because an import is a one-time migration that needs connected channels first, not a daily action. The onboarding's connected state ends with `Continue`, which leads to the Import step (a "migrating?" choice, then the form inline in the card); the Subscribers page header shows a primary **Import subscribers** button in the lookup field's slot only while the empty state is on screen (a tenant with rows gets the lookup field there and no import button); and Settings → Tenants has **Import subscribers** in every tenant's row menu (members included), for a second tenant or a later re-import. Every entry posts intent `import` with an explicit `tenant` to the Subscribers route's action (`lib/actions/subscribers.server.ts`, `subscribersAction`), so the handler exists once. A `FileDrop` (the onboarding component, now shared) takes a CSV; the browser parses it with `parseCsv` from `@buzzkit/schema/imports` and `detectPreset` names the provider from the header row, so a OneSignal export shows "OneSignal export" on the file and needs no mapping, while any other file asks for the external id column, the token or address column, what that column holds (Apple tokens, Android tokens, email addresses) and a switch to keep the remaining columns as attributes. Three selects apply when relevant: Apple environment, rows without an external id (skip, or import under the provider id, preset only) and unsubscribed rows (skip or import muted, preset only). `planImport` runs on every change and the summary lists rows to import, devices per kind, profile-only rows, provider-id and muted rows, then rows skipped per reason, every number a `NumberFlow`. **Import subscribers** describes its 100-row steps, accumulator and final copy to the root-level `components/jobs/provider.tsx`, then closes the dialog (or leaves onboarding) immediately. The generic background runner owns the keyed `useFetcher` and persistent progress toast across route navigation, and accepts one active job at a time; imports contain no lifecycle or toast orchestration of their own. Message sending and future multi-step work use the same definition rather than adding feature-specific providers to the root.
- **Detail** (`routes/[slug]/subscribers/[externalId]/index.tsx`): a profile. No page header (the list already showed the name and id) and no delete: a subscriber is not something you remove, you remove their subscriptions. Two columns (`minmax(0,1fr) 22rem`) inside their own scroller under the pinned back link: the left column scrolls, the right column is sticky at the top and, when taller than the viewport, scrolls on its own (`max-h-[calc(100dvh-8rem)]`, cards `shrink-0`); the page and the content panel never scroll here. Left: **Overview**, a definition-list card (Name, External id with the `Verified` badge, Email, Country with flag, City, Timezone with the local time, Language, Subscribed, Last seen, Verified); every textual value is a click-to-copy button with the press pattern, a clipboard glyph that appears on hover and swaps to a green check for 1.5s after copying (the code block's icon swap, no toast); then **Messages**, a `Table` of the latest deliveries addressed to this person (`GET /v1/subscribers/:id/deliveries`: title and body, channel, `DeliveryStatusBadge`, sent time); then **Activity**, the person's event stream (`GET /v1/subscribers/:id/timeline`, newest first) as rows with a tile, a plain-language label from `components/events/stream.ts` ("iOS device registered", "Preferences changed", or the custom name itself), where it came from ("from iOS", "from System"), and when it happened. Right: **Subscriptions** (mute / remove per device), **Preferences** (one compact row per topic: name and description, then a `ChannelBadge` for every channel the person currently receives it on, or quiet "Opted out"; editing lives behind a `…` menu of checkbox items, one per channel the topic offers, so a fifth channel adds a menu line, not another switch), **Attributes** (the customer's own keys only; name, email and system keys live in the profile card). Every empty section is a small `EmptyState`.
- All mutations are intents on the detail route's action (`lib/actions/subscribers.server.ts`: `subscription-enabled`, `subscription-remove`, `preference`, `delete`), fired through `useActionFetcher` so failures toast and React Router revalidates the page.
- The sidebar marks a section active for its nested pages too (`/subscribers/:externalId` keeps Subscribers lit); only Overview and sub-items (General, Members, …) match their path exactly, since sub-items share a prefix.

## Messages

- **List** (`routes/[slug]/messages/index.tsx`): header with a **Send test message** button, then the `FilterBar` (search over title, body, topic and target ids; Status (Scheduled, Queued, Sending, Completed, Canceled), Channel and Topic selects, the Time range with presets up to a year or a custom calendar range; Clear) driven by `useFilters`, then one card holding a `Table` over `GET /v1/messages` with its filters (newest first, keyset pages via `readPage` / `paginate`): Message (title with the body underneath, linking to the detail) · Channel (`ChannelBadge`) · To (a filled icon that says what kind of target it was: the tag for a topic, the single-person glyph for one subscriber, the Subscribers team glyph for several, then the topic slug / external id / "n subscribers" in the regular face, never mono; "n subscribers" lists the external ids in a tooltip (`components/messages/recipients.tsx`, shared with the detail page, where copying the row copies them one per line); a segment shows the target glyph and its slug) · Status (`MessageStatusBadge`: Scheduled sky, Queued amber like a pending delivery, Sending purple because it is the one state that is actively in motion, Completed green, Canceled gray) · Deliveries (only a 64px funnel bar, green sent / red failed + invalid / amber pending; the To column already says how big the audience was, and the numbers, reachable · sent · failed · pending, live in the bar's tooltip; a message with no deliveries shows the empty gray track, and its tooltip says "Scheduled for 2026-08-30 09:00 local time" while scheduled, "Deliveries scheduled" while queued, "No one was reachable" once completed or "Canceled before sending") · Sent (`TimeAgo`; a scheduled message shows the date it goes out as `Time` instead). Empty: `EmptyState` in the card with a curl snippet for `POST /v1/messages`; with filters active and no match, "No messages match" with a Clear button. The dialog (`components/messages/send-dialog.tsx`, shared with the segment page) starts with a Channel `Select` listing only the channels the tenant has a credential for, then To, a `Select` between Subscribers (external ids, comma separated), Topic (only topics offered on the chosen channel are listed) and Segment (the tenant's segments; hidden while there are none), then Title and Body, whose placeholders are one of ten matching example pushes ("Your order shipped" / "Arrives Thursday between 9am and 1pm."), picked afresh every time the dialog opens; at least one of the two is required. Under them a **Timing** select, Immediately or Scheduled; Scheduled adds a `datetime-local` input and a timezone `Combobox` (searchable: the IANA list is 400 entries) whose first entry is "Each subscriber's local time" (then the browser's zone, UTC and every IANA zone), the hint saying which of the two it means, and the button reads **Schedule message**. The message lands as Scheduled and the detail page opens. The API refuses channels it cannot send on yet (email) with `channel_unsupported`, which comes back as a toast. Intent `send` in `lib/actions/messages.server.ts` returns the new message's id and the dialog navigates to its detail.
- **Detail** (`routes/[slug]/messages/[id]/index.tsx`): the profile layout (pinned back link, two wheel-linked scroll columns) and the profile's parts. Left: **Overview**, the same `DetailRow` list as a subscriber profile (`components/detail/row.tsx`: fixed-width label, click-to-copy values with the hover clipboard glyph) for title, body, channel, "Sent to" with the target-kind icon, status, the schedule (wall-clock time with the timezone or "Local time" in `fg-2`; for a direct send the recipients' `$timezone` follows in brackets, the zone itself when they all share one, "n time zones" otherwise, read from the first 20 recipients) when there is one, created (labeled Sent for an immediate send) / expires / completed / canceled times and the idempotency key when set; while the message can still be stopped (Scheduled, or a subscriber-timezone send still releasing zones) the card action is **Cancel message**, an `AlertDialog` ("Delivery of this message stops. Deliveries already made stay as they are.", Keep / Cancel message); then **Deliveries**, whose header holds the title on the left and the `PillTabs` status filter on the right (buttons that navigate to `?status=`; All, Sent, Failed, Invalid, Retrying, Pending) over a `Table` of `GET /v1/messages/:id/deliveries` (subscriber id linking to the profile with its `PlatformBadge`, `DeliveryStatusBadge`, last error code, sent time or Queued / Retrying / Not sent, and a chevron). **There is no side sheet:** a row expands in place (`?delivery=dlv_…`, so the loader fetches `GET /v1/deliveries/:id/attempts` and the URL stays shareable) into the attempt ledger, a nested table with its own columns (Attempt · Outcome · Error · Time, deliberately not aligned with the outer columns): one row per attempt (number, outcome badge, error code with the provider reason and HTTP status in its tooltip, relative time · latency), and below it the selected attempt's request and response as code blocks side by side (the design system's plain `CodeBlock`, no extra border, so it is the same block as Payload). With several attempts, clicking a row selects it (radio-style, no second collapsible): the selected attempt's name reads in `fg-4`, the others in `fg-2`, no background; the latest attempt is selected by default. Clickable rows show the pointer on their whitespace too (`[&>td]:cursor-pointer`, since cells otherwise reset the cursor). The table uses a fixed layout with set widths for Status, Error, Sent and the chevron so the subscriber column truncates instead of pushing the table past the card. Deliveries that never reached the provider explain why in a small `EmptyState`. An empty deliveries list is a small `EmptyState` without a border, like the profile cards. Right: **Delivery**, with the `Funnel` bar in the header and the counts from `counts` as `NumberFlow` rows colored like the bar (Reachable in `fg-4`, Sent and Delivered green, Pending amber, Failed and Invalid red, gray while zero); then **Payload**, the stored message content (`payload` from `POST /v1/messages`: title, body, subtitle, badge, sound, image, `data`, collapse id, priority and the raw `apns` / `fcm` overrides, never the targets or channel) as a JSON `CodeBlock`. A Recipients / Topic card and a Timing card were tried and dropped: the delivery rows already link to every subscriber, and the two cards are enough for now.

## Events

Events is a sidebar group like Settings (Catalog, Stream), because the three cards of one page did not fit the rule that a list page never scrolls: the page pins one table and only that table scrolls, with its pagination always in view (Subscribers, Topics, Messages, API keys, the Audit log all work this way; the settings layout passes `min-h-0 flex-1` down so the audit log's table pins too).

`routes/[slug]/events/index.tsx` (Catalog): the tenant's event dictionary ([api/events.md](api/events.md)), tenant-scoped like Messages. Header ("Events", "Every event your app and server track, and how often."), then **Volume**, a bklit `AreaChart` of events per bucket (`components/events/volume-chart.tsx`, sky line, empty buckets filled client-side so the line runs edge to edge) with `PillTabs` for the range (24 hours, 7 days, 30 days → `?range=`; hour, six-hour and day buckets, ticks and tooltip titles per bucket size), and under it the pinned catalog table: one single-line row per name from `GET /v1/events/names` (`components/events/name.tsx`: the glyph and label from `stream.ts`, never the raw `$` name; counts for 24h and 7d and 7-day users rolling with `NumberFlow`, one `SourceBadge` per source, last seen as `TimeAgo`), the row linking to the name's page. A tenant with no events shows the `EmptyState` with the `curl` for `POST /v1/events`.

`routes/[slug]/events/stream/index.tsx` (Stream): the newest events first, 50 per page through the shared `readPage` / `paginate` / `TablePagination` stack over the API's cursor, filtered with a `FilterBar` (Event: every catalog name; Source: Server, iOS, Android, Web, System → `?event=` / `?source=`). Rows: event (glyph and label), subscriber linking to the profile, source, the data summarised as `key: value · …`, time, a chevron; a row expands in place into the JSON `CodeBlock` exactly like the audit log. The first page is always live, a `LivePing` beside the title: the browser polls Tinybird's `event_recent` endpoint every three seconds with the tenant-pinned JWT the loader fetched from `GET /v1/events/token` (the loader skips the token on later pages) and prepends what arrived, honoring the active filters. This is the dashboard's one client-side fetch, by design ([engine.md](engine.md)): the API is never polled, and the token cannot read another tenant.

`routes/[slug]/events/[name]/index.tsx`: one name. The back button (the detail pages' ghost button) and the header (the label, the mono name for reserved events, first and last seen) with the range `PillTabs` stay pinned; below them one fading scroll column holds four tiles (24h, 7d, 30d, users 7d), the volume card with the sources as badges in its action, then, split two ways under the chart (`minmax(0,1fr) 22rem`, each card its own height), **Recent** (the 20 newest events of this name, expandable) beside **Fields** (every key seen in the samples' `data`, its types and an example value, the shape a `where` clause or a template can reference).

`components/events/stream.ts` is the vocabulary of the stream: `describeStreamEvent(event)` gives every `$` name a label, an icon and an optional detail derived from `data` ("iOS device registered", "Email address added", "gym push off"), and shows a custom name as itself with a zap; `SOURCE_LABELS` names the sources (Server, iOS, Android, Web, System: the engine's own events); `summarizeData` flattens a payload for a table cell. The subscriber profile's Activity feed renders through the same module, so an event reads identically on the profile, in the tail and on its page.

## Audit log

`routes/[slug]/settings/audit-log/index.tsx` (Settings → Audit log): the workspace ledger of control-plane changes (`GET /v1/workspaces/:slug/audit`, admin sessions), the same shape as Messages. Header, then the `FilterBar`: **Event** (a grouped select of every catalog name with its plain-language label: Workspace, API keys, Tenants, Credentials, Topics, Messages), **Actor** (Member, API key, BuzzKit), **Time** (`FilterRange`: the shared `RANGES` presets from `use-filters.ts`, up to the last 12 months, or a custom day range from the calendar, also used by Messages), Clear, and search (`?q=`, matched by the API against the event name, the actor's display and the target id). Then one card with a `Table` kept as dense as a transaction log, one line per row: **Event** (a 16px filled glyph and the label from `components/events/describe.ts`) · **Details** (the derived detail, "7 sent · 5 failed", "Changed name, slug", or the quiet raw event name when there is nothing to add; the column takes the remaining width and truncates) · **Actor** (a glyph for the kind, `IconUserFilled` member / `IconKeyholeFilled` key / the BuzzKit mark for the system, then the display) · **Target** (a glyph for the kind and the id, linking where a page exists: messages, topics, keys) · **Time** (`TimeAgo`) · a chevron. No tiles, no second lines: the ledger is scanned, not read. Clicking a row expands it in place (client state only, nothing to fetch) into the profile's `DetailRow` list running edge to edge under the row, so labels share the table's left padding (event name, event id, actor, target, request id, IP address, client, every value click-to-copy), followed by a **Data** block (the label on its own line, the JSON `CodeBlock` full width beneath it, in the same 16px padding). The table is `table-fixed` (Event, Actor and Target at set widths, Details taking the rest) so every cell truncates inside the card instead of scrolling it sideways. Empty: "No changes yet" in the card; "No changes match" with a Clear button when filters are active.

`components/events/describe.ts` is the one vocabulary for the ledger: `describeEvent(event)` gives every catalog name a label, an icon and an optional detail derived from `data`, `actorLabel(event)` phrases the actor ("by name@…", "via CI (bk_ws_…)", "by BuzzKit"), and `EVENT_GROUPS` / `EVENT_NAMES` feed the filter.

## Overview

`routes/[slug]/index.tsx`, the tenant's front page, built on `GET /v1/stats` (one round trip, [api/stats.md](api/stats.md)) plus the five newest messages and subscribers. The header reads **Overview** (the workspace name already sits in the switcher) with "Track subscribers, messages and deliveries over time.", and on the right a `FilterRange` (the shared presets, default **Last 7 days**, or a custom calendar window; `?range=` like every other list). Then:

- **Six stat tiles** in a 3 × 2 grid (two per row below `lg`): **Subscribers** (every live subscriber, with the period's growth), **Messages**, **Delivered**, **Failed**, **Events** (tracked in the period by the product and its users, engine bookkeeping excluded) and **Runs** (workflow runs started in the period), each with its delta against the previous window and a sparkline from the same `series`; a **Scheduled** strip below them while messages are waiting, naming the next send and linking to the filtered Messages list.
- **Deliveries**, a card with a bklit `AreaChart` of the period: two lines from the API's `series`, sent in `green-4` and failed (incl. invalid) in `red-4`, 2px strokes with a fill that fades to nothing at the base (the same shape as the tile sparklines, so the page reads as one family), `xDomain` pinned to the first and last day so the lines run edge to edge, three solid hairline gridlines, an `XAxis` whose labels stay put while hovering (`tickerHalfWidth={0}`) and sit 6px clear of the baseline (`offset={6}`: the labels move down towards the chart's bottom edge, the plot keeps its 28px bottom margin), and bklit's `ChartTooltip` without its date pill (crosshair on), listing Sent and Failed for the hovered bucket with values that roll through `NumberFlow`. **The axis adapts to the window.** The API buckets `series` by `interval` (hour up to two days, day up to 120, week up to 200, month beyond; `docs/api/stats.md`) and `axisPlan` in the route turns that into which points get a label and how they read: 24 hours label every fourth hour in the viewer's local time ("4 AM", "12 PM"); a week labels every day by weekday name with the current day as "Today"; up to two weeks every other day and beyond that only Mondays ("Aug 3"), every other Monday past two months; weekly buckets label every other week by the Monday they start; monthly buckets label every month by name, with the year on January, always two digits ("Jan ’26"). The tooltip title follows the bucket, with the qualifier in the muted tooltip color and the value in the foreground: "Aug 25 4 PM", "Fri Aug 14", "Week of Aug 3", "April ’26". Switching ranges morphs every chart from the old shape to the new one inside the plot (`yDomainTween={false}` plus the design system's path morph: full width from the first frame, only the heights move), so a range with a taller series never spills in from above the sparkline and a narrower range never fills in from the left. The card description names the bucket ("per day", "per month"). Bars were tried first and dropped: with a week of a few sends they stood as lonely pegs on an empty plain, while lines still draw a shape; the bar chart code was deleted with them (`bunx shadcn add @bklit/bar-chart` brings it back). A legend (Sent · Failed dots) sits in the card action; with no deliveries in the period the card holds a small `EmptyState`. Status colors are the delivery colors used everywhere else; text never wears them. **Events** and **Runs** follow side by side with the same chart (events per bucket in `amber-4`; runs started in `blue-4`, completed `green-4`, failed `red-4`), then the four mini tables: Recent messages, New subscribers, Top events (the period's five most tracked names, linking to the event page) and Active workflows (live runs per workflow, linking to the workflow).
- **Recent messages** and **New subscribers**, two half-width cards holding mini versions of the Messages and Subscribers tables (the same `Table`, the same rows, only the most important columns): Message (title and body) · Status · Sent, and Subscriber (avatar, name with the country flag beside it, external id or email) · Channels (platform and channel badges) · Subscribed. The first cell links to the detail exactly as on the list pages (no hover underline, focus ring only); `attribute` and `countryName` moved to `components/subscribers/attributes.ts` so both pages read attributes the same way. Each card has a title-only header exactly as tall as the profile page's (`CardHeader className='py-3'`, 42px) with a **View all** action into its list page, and a small in-card `EmptyState` when empty. A `CardAction` is positioned absolutely at the header's right edge and centered on its height, so it neither adds height nor shifts a title-only header; the header reserves `pr-28` for it so a long description never runs underneath (an earlier two-column grid put the description beside the title once the action left the grid flow, the Overview's Deliveries card showed it). Plain list rows with an underlined title link were tried first and dropped: not the dashboard's selection language.
- **Quickstart** replaces all of the above while the tenant has never sent a message (`messages.length === 0` on the five newest, so the switch is not tied to the range): a page of six empty tiles and eight "No … yet" cards is the wrong first screen, and this is what **Open dashboard** at the end of onboarding lands on. The page header becomes **Quick start** ("Connect a channel, add BuzzKit to your app and send your first message.") with no range control, and the body is three stacked cards in the settings-card grammar with plain titles (no step numbers or markers: the order of the cards is the sequence, and each card's badge, live dot or button says where it stands): **Connect a channel**, shown only while the tenant has no credential, which onboarding makes impossible for the default tenant and so in practice means a fresh non-default tenant (one `SettingsRow` per available provider, the Apple tile and its description, ending in a soft **Connect** link into Settings → Channels), **Add BuzzKit to your app** (the same `IntegratePanel` as the onboarding's Integrate step; its description, the `LivePing` waiting line, the green done badge, the footer sentence and the guide link all come from `APP_GUIDES`, picked by the tenant's connected channels: no channel yet (a fresh non-default tenant with the Connect card above) reads the neutral "Add the SDK and identify your users", "Waiting for the first subscriber" and links the docs, so nothing is push-pilled before a provider is chosen; push only reads "register for push", "Waiting for the first device", "Device registered", "four lines of Swift" and links the iOS guide; email only reads "identify your users with their email address", "the first subscriber", "one npm install" and links the quickstart; more than one channel reads "register their devices and addresses", "the first subscription" and links the docs. The client-key field never vanishes: the live key in a `CodeBlock`, a same-size skeleton while loading, and a soft **Create a client key** link to the keys page when the tenant's auto-created key was revoked, since a missing field read as a bug), then **Create a workspace key** (almost every backend needs one and keys are session-only, so the step makes it: a header, no content, a footer that says "Full access, named “Backend”. Rename or narrow it under API keys." beside **Create workspace key**, which posts intent `create-key` to the route's action and opens the shown-once `CreatedKeyDialog` (`components/keys/created.tsx`, the same view the API keys page uses, locked until the key is copied); once a live workspace key exists the card shows a green `Key created` badge, the sentence "Every key is listed under API keys." and a ghost **Open API keys** link; a key created on this page is also injected into the send card's curl in place of the `bk_ws_…` placeholder, held only in React state so a reload forgets it, never in storage, and the send footer says so while no device has registered), and **Send your first message** (the `POST /v1/messages` curl with a `bk_ws_…` placeholder, since secret keys are never readable, carrying `"channel"` when the tenant's connected channels do not include push, so an email-only tenant's snippet works as pasted, over a footer whose **Send test message** button is disabled until a subscriber holds a subscription on one of the tenant's connected channels (a backend identify alone does not count: the list item's `channels` must intersect `connected`) and then really sends on that channel: it posts intent `send` to the route's own action (`quickStartAction` in `lib/actions/quick-start.server.ts`, which dispatches `send` to the messages `sendIntent` and `create-key` to the keys `createKeyIntent`) with a fixed title and body addressed to that subscriber's external id, and lands on the new message's page so the deliveries are the next thing seen; the footer sentence names that subscriber, or says the button is enabled once the first device has registered). The loader fetches the client key only in this state, the way the Subscribers empty state does. Because the Overview is live, a device registering flips step 2 to done without a reload, and the first message swaps the whole page for the tiles and charts. **The first paint is already right, server-rendered.** The workspace layout's loader carries a `quickstart` hint the same way it carries the role hint: a `buzzkit.quickstart` cookie keyed by workspace and tenant. While the hint is missing or `true` the loader awaits one `GET /v1/messages?limit=1` and the tenant's credentials and rewrites the cookie; once it is `false` it is never checked again, so a tenant that has sent never pays for it. The value reaches the sidebar (its first item reads **Quick start** instead of **Overview** on every page of that tenant) and a `QuickStartProvider` (`hooks/use-quick-start.tsx`, carrying `{ quickstart, connected }`, so the server-rendered app card already has the right channel copy) around the page slot, so the Overview route's handle skeleton, which the layout paints before the page can mount and on the server, already renders the Quick start header and the real quick-start cards instead of the tiles: the cards are static except for what the data decides, so the skeleton is the same `AppCard` and `SendCard` with one rounded rectangle where the client key goes (a `Skeleton` around an invisible 46-character placeholder set in the code block's own padding and type, so it measures exactly what the key will, to the subpixel, with no fixed height to drift), the copy button and the send button disabled, the live dot showing, and the curl real (the API origin comes from the layout's loader data through `useRouteLoaderData`); no line skeletons anywhere. The route's own data still decides the body, and overrides the header once it lands (`view` state), so a message sent from outside the dashboard between two layout loads shows the Overview under the right header after one refresh; the layout loader re-runs on a form post (the test send included) or a workspace or tenant change, which is when the hint flips.
- **Simulating the flow locally** needs no device: create a tenant (Settings → Tenants, or post intent `create` to that route), connect a throwaway Apple key to it (a PKCS #8 key from `openssl ecparam -name prime256v1 -genkey | openssl pkcs8 -topk8 -nocrypt`, any 10-character Key ID and Team ID; local `wrangler dev` cannot reach APNs, so it is stored `unvalidated`, which counts as connected), open the Overview for that tenant and copy its client key, then from a shell `POST /v1/client/identify { externalId }` and `POST /v1/client/subscriptions { externalId, channel: "push", platform: "ios", token: <64 hex>, environment: "sandbox" }` with `Authorization: Bearer bk_pk_…` against `localhost:8790`; the app card flips to `Device registered` on the next live refresh, **Send test message** enables, and clicking it creates a real message (its delivery fails at APNs, which the message page shows) and leaves quick start for good. The send fetcher lives on the route component, not inside the quick-start cards: the send's revalidation unmounts the cards before their callbacks run, so a fetcher declared there never navigates.
- No setup checklist and no Channels card once the tenant has sent: both used to sit here and both are gone (providers live in Settings → Channels, the API snippets live in the docs, and the Overview is about what happened, not configuration). The one configuration nudge left is a **No channel connected** banner above the tiles (a compact row: icon tile, "No channel connected" with "Connect a channel before this tenant can send.", and a **Connect channel** button into Settings → Channels), shown only when the tenant has no credential, which in practice means a fresh non-default tenant, since a workspace whose default tenant has none is redirected to onboarding.

Charts come from the bklit registry (`@bklit/*`, visx + motion) installed into `packages/ui/src/components/charts/` the shadcn way and restyled to the tokens; see [design.md](design.md) → Charts.

## Settings

Settings' sub-items are sidebar entries (General, Channels, Tenants, Members, Billing `Soon`); each page has the standard header and stacks cards.

- **General** (`routes/[slug]/settings/index.tsx`): the Workspace (name) and Slug `SettingsCard`s, and for owners a **Delete workspace** card (destructive `xs` button in the footer) whose `AlertDialog` explains the consequence, requires typing the slug into a field, and on confirm soft-deletes through `DELETE /v1/workspaces/:slug`, clears the last-workspace cookie and lands on `/` (which picks another workspace or the onboarding). Intents `update`, `set-slug`, `delete` in `lib/actions/workspace.server.ts`.
- **Channels** (`routes/[slug]/settings/channels/index.tsx`): one `Card` per available channel from the onboarding catalog (Push notifications, Email), listing every provider as a `SettingsRow` (provider tile, name with a `Sandbox` badge when an APNs sandbox credential exists, the credential details "Key ID … · Team ID … · Bundle ID …" or the provider's description underneath, or the provider's last error when validation failed). Connected providers show "Checked <TimeAgo>" (the newest `validatedAt`), the `CredentialStatusBadge` (the worst status across the provider's credentials) and a `…` menu: **Validate again** (`POST /credentials/:id/validate` for each of the provider's credentials; a rejection comes back as a toast with the provider's reason), **Replace** and **Remove** (`AlertDialog`, "Sends through it stop immediately. Topics and subscribers stay."). Unconnected providers get a soft **Connect** button; unavailable ones are dimmed with `Soon`. Connect and Replace open the **same guide card as a dialog** (`components/onboarding/provider-dialog.tsx`): `ProviderDialog` renders `GuideCardBody`, the header + animated step content + footer that `OnboardingLayout` also places inside its `Card` (`components/onboarding/layout.tsx`), inside a `DialogContent` stripped of its padding and sized like the onboarding card, with the dialog's own title and description elements standing in for the card's so the dialog stays labeled. It drives the same `useProviderGuide` hook with `back` as a callback (closing the dialog on the first step), `trackStep: false` (no `?step=` in the URL) and `action` pointed at the Channels route, whose action handles `connect` and `validate` through `connectProvider` (shared with the onboarding action in `lib/actions/connect.server.ts`; the guide form carries a hidden `provider`) and `remove` (comma-separated ids). Steps slide inside the dialog exactly as in the onboarding, the connected state follows with Validate again and a **Done** button that closes it, and the page revalidates so the row shows the new credential. Uploading replaces the provider's existing credential (`replaceCredentials` in the API). The page shows the selected tenant's providers.
- **Members** (`routes/[slug]/settings/members/index.tsx`): header with **Invite member** (admins and owners), a `Table` of members (avatar: the uploaded image or the person's orb, name with a quiet "You" on your own row and the email underneath · `RoleBadge` · Joined) with a `…` menu on rows you may act on (admins on members and admins, owners on everyone but themselves): **Change role** as a submenu of radio items (Owner only offered to owners) and **Remove** (`AlertDialog`: they lose access immediately, you can invite them again). Below it an **Invites** card (title and description, then a table of pending invites: email · role · Invited · Expires as a date, with **Send again** and **Revoke** in the menu; an in-card `EmptyState` when none). The invite `Dialog` takes an email and a role (Member or Admin, with one sentence per role describing what it adds) and sends; when the API reports `emailSent: false` (no email binding locally, or delivery failed) the dialog switches to a **Share the invite link** view with the `/invite/:token` link and a Copy button. The API refuses duplicates ("They already have access or a pending invite.") and owner escalation by admins. Intents `role`, `remove`, `invite`, `resend`, `revoke` in `lib/actions/members.server.ts`.
- **Tenants** (`routes/[slug]/settings/tenants/index.tsx`): header with **Create tenant**, one `Table` (name with a `Default` badge on the default tenant and the slug underneath · id · Created) and a `…` menu with **Edit** (the same `Dialog` as create: name, slug auto-derived until touched, locked for the default tenant) and **Delete** (never offered for the default tenant; `AlertDialog`: subscribers, credentials and keys stop working immediately). Intents `create`, `update`, `delete` in `lib/actions/tenants.server.ts`. The switcher lives in the workspace switcher (above).

## Topics

`routes/[slug]/segments/index.tsx` (Audience → Segments): a pinned table of the tenant's segments (name with the description or slug under it, the conditions as one line of `describeExpression` phrases such as "plan is pro · order.completed at least 1× in the last 30 days", `v2`, updated), each row linking to its page; **New segment** (admins) links to `segments/new`. Empty: `EmptyState` in the card. `routes/[slug]/segments/new/index.tsx` and `routes/[slug]/segments/[segmentSlug]/index.tsx` render one `components/segments/editor.tsx` (ghost back button, header with the name and description, Save + a `…` menu with Send to segment and Delete on a saved segment, one scrolling column of three cards). First **Details** (name, slug auto-derived until touched and fixed after creation, description). Then **Conditions**, with `PillTabs` Builder / JSON. The builder (`components/segments/builder.tsx`) is a "Subscribers matching all / any of these conditions" line over rows, each a kind `Select` (Attribute · Did event · Never did event · Activity · Channel) followed by its fields (attribute key, comparison and value; event name with a datalist of the catalog, at least / exactly / at most / more than / fewer than, a count and a window; event and window; active or not active in the last window; can receive push or email), a trash button, and **Add condition**; rows animate in and out. `components/segments/expression.ts` converts rows to the API expression and back; an expression the builder cannot represent (nested groups, `not`, several comparators on one ref, unusual durations) opens in the JSON tab, which validates as you type: a location-aware JSON parser (`components/segments/json.ts`) turns syntax slips into one line-and-column message in plain words (trailing commas, unquoted keys, single quotes, comments), and `lintExpression` from `buzzkit/expressions` lists every semantic problem with its path, line and a message that says what is allowed; clicking a problem selects its line in the textarea. Deleting the last condition is not possible, and a saved segment shows an amber "Unsaved changes" badge with a Discard button as soon as anything differs from what is saved. Then **Matching now**, the count as `NumberFlow` in the card header with a spinner while a preview is in flight, over the same subscriber table as the Subscribers page (`components/subscribers/table.tsx`: avatar and name, country, channels, subscribed, last seen) for up to 20 members; every edit posts intent `preview` to the route action 400ms after the last keystroke and keeps the last good result while the next one loads, so the number never flickers to nothing; a row that is still being filled in is left out of the preview (Save still needs every row complete). Save is enabled only when something changed and every row is complete; creating navigates to the new page, deleting (behind an `AlertDialog`) back to the list. **Send to segment** opens the shared `components/messages/send-dialog.tsx` with the segment preselected. Intents `preview`, `create`, `update`, `delete` and `send` live in `lib/actions/segments.server.ts`.

`routes/[slug]/topics/index.tsx`: header with a **Create topic** button, then one card holding a `Table` (Topic: name with the description underneath · Slug in mono · one column per connected channel (a channel the tenant no longer holds a credential for gets no column, even if old topics still offer it), with an `OptInBadge` showing the *resolved* default, `Opted in` green / `Opted out` gray, or quiet "Not offered" text when the topic does not include that channel · Created via `Time` · a `…` menu with Edit and Delete), paginated with `TablePagination` over the API's keyset pages (`readPage` / `paginate`, 50 per page, newest first). Empty: `EmptyState` inside the same card ("No topics yet"), like API keys. Create and edit share one `Dialog` (name, slug auto-derived until touched, description, an "opted in by default" switch (`defaultOptedIn`), then a **Channels** block with one row per channel that is *connected* in the tenant (has a credential); when editing, a channel the topic still offers but whose credential is gone is listed too, flagged "Not connected anymore. Subscribers cannot receive it here.", so it can be set to "Not offered" (with nothing connected every channel is listed). Each row is one `Select`: "Follow topic default" / "Opted in" / "Opted out" (offered, mapping to `channelDefaults`) or "Not offered" (left out of `channels`); at least one channel must be offered, and new topics start with every listed channel on "Follow topic default". The submit button is `loading` while the action runs. Delete sits behind an `AlertDialog` ("Delete “Deals”?", sends stop and subscribers no longer see it, cannot be undone, action "Delete topic"). Intents `create`, `update`, `delete` live in `lib/actions/topics.server.ts`; a slug conflict comes back as a toast.

## Webhooks

`routes/[slug]/webhooks/index.tsx` (Developers → Webhooks): the endpoint list as a pinned table (endpoint URL with its description under it, `EndpointStatusBadge` Active / Failing / Disabled, tenant or "All tenants", the subscription summary, created), each row linking to the endpoint page; **Add endpoint** (admins) opens a dialog: URL, description, tenant (only shown with more than one), then Events as "Every event" or "Pick events" with the `ScopePicker` over the API's catalog groups (`GET …/webhooks/catalog`: audit resources and the `$` stream groups, labels without the `$`) plus a free-text field for the tenant's own names (`order.completed, order.*`). Creating ends on the signing secret in a `CodeBlock` with a button into the endpoint page (the secret stays readable there, unlike API keys).

`routes/[slug]/webhooks/[id]/index.tsx`: the detail-page pattern (ghost back button, header with the URL and its status badge, an icon `…` menu with Edit, Enable / Disable, Rotate secret and Delete, two wheel-linked scroll columns). **Edit** opens the same form as creation (`components/webhooks/endpoint-form.tsx`: URL validated as you type, description, tenant when the workspace has several, every event or a picked set plus your own names) prefilled. Left: **Deliveries**, a pinned table with `PillTabs` (All, Delivered, Retrying, Failed, Pending → `?status=`): event type, `WebhookStatusBadge` (Pending purple like a sending message, Retrying amber, Delivered green, Failed red), attempts, last response status, last attempt; a row expands through `?delivery=` into the message page's attempt ledger: a sub-table of attempts (number, `WebhookAttemptBadge`, error, time and duration), a **Resend** row for admins while the endpoint is enabled, then the selected attempt's Request (the signed payload) and Response (status line, then the body pretty-printed when it is JSON, up to 4 KB) side by side; paged with the shared stack. Endpoint badges: Active green, Failing red, Disabled gray. The page revalidates itself while a delivery is pending or a retry is due (three seconds, or the retry's time, never more than a minute apart), so statuses and attempt counts move live. Right: **Overview** (URL, tenant, the subscription as "Every event", one mono name, or "3 events" with the names in a tooltip, the same `EventsSummary` as the list, created, failing since, disabled with the reason) and **Signing secret** (masked until Reveal, click-to-copy, the previous secret with its expiry while a rotation overlaps).

`routes/[slug]/sources/index.tsx` (Developers → Sources): the tenant's sources as a pinned table (name with the count of mapped event types under it, `SourceProviderBadge` Stripe / Superwall / Custom, `SourceStatusBadge` Unverified amber / Active green / Paused gray, last delivery, created), each row linking to the source page. **Add source** (admins): name and provider (a `Select` over the presets with their logos and a sentence on what the preset does); creating toasts and opens the source page with `?setup`, where the setup dialog takes over.

`routes/[slug]/sources/[id]/index.tsx`: the detail-page pattern (back button, name with its status badge, the ingest URL as the description, a `…` menu with Edit (name, verification scheme as Stripe signature / Standard Webhooks headers / Svix headers / shared secret header with its header name, new secret), Pause / Resume (never on an unverified source) and Delete). Left, in reading order: an unverified source's one-line card with **Add secret**; **Mapping**; **Try a payload**; **Deliveries** last, a pinned table with `PillTabs` (All / Events / Duplicates / Dropped / Rejected / Unverified → `?outcome=`), paged with the shared stack; an unverified delivery whose payload is recognized offers **Set up as Stripe** (an `AlertDialog`, then the preset's provider, verification and mapping replace the source's, the secret kept) (provider type, `IngestOutcomeBadge` Event green / Duplicate gray / Dropped amber / Rejected red / Unverified purple, the result as an event chip, a drop reason or the rejection detail, time) whose rows expand into the detail sentence, the raw payload, **Use as sample** and, on a delivery dropped for an unlisted type, **Map `<type>` as an event** (one click adds it to the mapping and saves); **Mapping**, read as rows (event type, event id, time, subscriber and the attribute it is matched to, the `where` as condition chips, the data keys kept, the provider type → event name list or "Every type, under its own name") with **Edit** swapping in the JSON editor linted inline with `@buzzkit/schema/sources` (Save mapping / Cancel); the payload tester shows a sample textarea beside what it becomes (event name, subscriber, data, or the drop reason), rendered live in the browser with `mapPayload`, followed by suggestions from `suggestMapping` and the outcome ("The event type is not at `type`. It looks like `event.type`." → **Read it from `event.type`**, "`charge.refunded` is not mapped to an event" → **Map it as an event**, "This payload looks like Superwall"), each a one-click save, and a payload recognized as another provider offers **Set up as ‹Provider›** into the same preset dialog; suggestion rows separate with full-width dividers. Right: **Overview** with only facts (endpoint URL with copy, provider logo and label, Verification as a Verified / Unverified badge whose tooltip names what deliveries are checked against, the secret as a Set / Missing badge, last delivery, created). The **setup dialog** ("Point Stripe at this URL": the full ingest URL in a `CodeBlock` to paste at the provider, then the secret the provider shows for it, plain input, password managers ignored, the hint following the source's verification scheme) ends in a `DialogFooter` with **Add the secret later** and **Activate source**; opened by `?setup` right after creation it has no close button and ignores Escape and the backdrop, closing only through its two buttons (which also drop the param); the unverified card's **Add secret** opens the same dialog, closable this time. Provider is shown as its logo (Brandfetch, from the preset's `domain`; a filled mailbox tile for custom) with a plain label, never a colored badge.

## API keys

`routes/[slug]/keys/index.tsx` (Developers → API keys), feedbase's keys page 1:1: one `Card` listing every key as a `SettingsRow`. Each row shows the name with a kind badge (**Workspace** `bk_ws_`, every tenant · **Tenant** `bk_tn_`, locked to one tenant · **Client** `bk_pk_`, public, `/v1/client/*` only), a mono `prefix…last4` (client keys show the full token), the scope summary ("Full access" or "3 scopes"), the tenant, and last used / created. Actions sit in a `…` dropdown (`IconDotGrid1x3Horizontal`): **Copy key** for client keys, **Revoke** for admins, which opens one page-level `AlertDialog` ("Revoke {name}?"). Revoked keys keep their row with a red badge. Empty: an `EmptyState` inside the card. The list is paginated 50 per page through the API's cursor (`?cursor=`) with the shared pagination stack (below); the page never scrolls: the route fills the content panel, the page header and the table header stay put, the table body scrolls inside the card, and, only when there is more than one page, a footer row (same height as the header) carries "Page 2 / 3" (both numbers roll with NumberFlow; the page count comes from the list's `total`) and Previous / Next with 16px chevrons. Previous works without backward paging in the API by carrying the visited cursors in `?trail=`.

One **Create key** dialog: name, type (with a one-line hint per type), tenant (for tenant and client kinds), and for secret kinds a permissions preset (Full access = `*`, Read only = every `:read` the kind can hold, Custom = `ScopePicker` from `@buzzkit/ui` over `resource:*` wildcards and individual scopes, tenant keys see only tenant-context groups). Whenever a form has a tenant select it starts on the **default tenant** (`isDefault`), never on the first row of the list.. On success the dialog switches to the **shown-once** view: the key in a `CodeBlock`, and under "Use it right away" the identify `curl` (or the client identify call) **already carrying the key**, which is the only moment the dashboard ever holds the plaintext. Intents `create` and `revoke` live in `lib/actions/keys.server.ts`.

## Overview pass (phase 7) — plan

One window for everything: `GET /v1/stats?from&to&interval` grows, the page stays one loader.

**API (`api/stats`)**, all over the same `range` and `interval`, with the previous window for the deltas:
- `events: { total, series }` from Tinybird `event_volume` (bucket = the interval), and `topEvents: [{ name, count }]` (top 5 in the window) from a new `event_top` endpoint over `event_names_hourly`.
- `runs: { started, completed, failed, series: [{ bucket, started, completed, failed }] }` from a new Tinybird `run_volume` endpoint over `runs_current` (runs bucketed by `started_at`, split by their current status), plus `workflows: [{ slug, name, status, running, sleeping, waiting }]` for the active workflows (Postgres + `run_counts`).
- `scheduled: { count, nextAt }` from Postgres (`status = 'scheduled'`, the earliest `scheduled_for`).

**Page (`/:slug`)**, same cards and rhythm as today:
- Tiles become six in a 3 × 2 grid: Subscribers, Messages, Delivered, Failed, **Events** (count, sparkline), **Runs** (started, sparkline; the qualifier shows completed / failed).
- A **Scheduled** strip under the tiles when anything is scheduled: "3 messages scheduled, the next one Tue 09:00" linking to Messages filtered by status.
- Charts: Deliveries (as is), then **Events** (the shared `volume-chart`) and **Runs** (started / completed / failed per bucket, the delivery chart's style) side by side.
- Mini tables: Recent messages, New subscribers (as is), **Top events** (name, count, linking to the event page) and **Active workflows** (name, the three live counts, linking to the workflow).

**Done when** the Overview reads as one dashboard over the window: every tile, chart and table agrees with its own page for the same range; an empty tenant shows the same empty states the pages do; Tinybird lag is the only difference between the Overview and the live pages.

## Paged tables

Three layers, so a route never computes page numbers or links:

1. **API page** — every list is `{ items, hasMore, nextCursor, total? }` (`docs/api/conventions.md`).
2. **`lib/utils/pagination.ts`** — `readPage(request)` reads `?cursor=` and returns the `{ limit, cursor }` query for the API; `paginate(request, page)` returns `{ items, pagination }` where `pagination` is `{ page, pageCount, previous, next }`: the page number counted from the cursor trail in the URL, the page count and total from `total`, and Previous / Next as hrefs that keep every other query param. Previous exists without backward paging in the API because the visited cursors ride along in `?trail=`.
3. **`TablePagination` from `@buzzkit/ui`** — `<TablePagination {...pagination} />`, composed as the last child of `Table` like `TableHeader` and `TableBody`; `Table` lifts it out of the scroll viewport so it never scrolls, in either direction. It navigates through the design system's `LinkProvider`, which `root.tsx` hands react-router's `Link` once for the whole app (`<LinkProvider link={Link}>`). The row disables Previous on the first page and Next on the last (or when there is no next cursor), reads "Page 2 / 3 (131)" with rolling numbers, and renders nothing for a single page.

A paged route is therefore: `const page = await listX(ctx, token, slug, readPage(request)); return paginate(request, page);` in the loader, and `<Card><Table><TableHeader>…</TableHeader><TableBody>…</TableBody><TablePagination {...pagination} /></Table></Card>` in the view.

## Phases

Each phase ends in something reviewable in the browser. A phase starts only after the previous one has been reviewed and approved.

| Phase | Scope | Status |
|---|---|---|
| **1. Foundation + onboarding** | `@buzzkit/ui`, `@buzzkit/eden` + API contract, auth pages, workspace creation, floating shell (switcher, pill nav, account menu, theme), overview with channel card + setup checklist, Settings → General, the full onboarding flow with the APNs / FCM / Resend guides, `/ui` preview, docs | ✅ built, awaiting review |
| **2. Settings** | **Channels**: credentials of the selected tenant (status, environment, last check, validate again, revoke, replace via the guide). **API keys** ✅ (its own page under Developers): workspace / tenant / client keys with the scope picker (presets + custom), shown-once secrets, client-key token copy, revoke. **Members & invites** ✅: roles, remove, invite by email with resend/revoke, the owner guard. **Tenants** ✅ (list / create / rename / delete, and the switcher in the shell, cookie-persisted + `?tenant=`; metadata editing still to come). **Channels** ✅. **Delete workspace** ✅ (General, owners). **Audit log** ✅ (Events under Activity): `GET /workspaces/:slug/events` with event, actor, time and search filters, rows expanding into request details and data | planned |
| **3. Subscribers & topics** | Subscribers list (keyset pagination, lookup by externalId, verified badge) ✅ · subscriber detail (attributes, subscriptions with mute/unmute/remove, per-topic × channel preferences, delete) ✅. **Topics** (its own page under Audience): CRUD ✅, default opt-in, per-channel defaults | planned |
| **4. Messages** | Messages list with status and funnel counts ✅ · message detail with the filterable delivery table and the in-place attempt ledger ✅ · "Send test message" dialog (channel, `to` / `topic`, title, body) ✅. Still to come in the composer: `data`, TTL, channel, APNs environment | in progress |
| **5. Tenant settings & hardening** | Identity verification toggle + identity secret reveal/rotate, channel kill-switches, danger zone (delete tenant / workspace), email verification on sign-up (`requireEmailVerification` + the email lib), workspace avatar, onboarding checklist steps 2-4 becoming live once keys/subscribers/messages pages exist | planned |
| **6. Engine pages** | Events (stream, catalog, volume, live tail, per-name page), the audit log under Settings and the profile timeline ✅ (E1) · Webhooks ✅ (E2) · Segments ✅ (E3) · scheduled messages ✅ (E4, inside the Messages pages) · Workflows ✅ (E5: the list with live counts, the workflow page with Steps / Code / Versions / Runs, the run page, Runs on the profile) · schedule-triggered workflows come with E6; a visual builder is deferred until all workflow phases are done, and not mandatory ([engine.md](engine.md)) | E1–E5 built |
| **7. Overview pass** | Built 2026-08-29 (after E5): the Overview grew beyond sends on the same `GET /v1/stats` window: Events and Runs tiles with sparklines, a Scheduled strip, Events and Runs charts beside Deliveries, Top events and Active workflows mini tables. | Built |

## Configuration

| Var | Where | Meaning |
|---|---|---|
| `API_URL` | `wrangler.jsonc` `vars` (dev default `http://localhost:8790`; production overrides at deploy) | API origin |

The API's `DASHBOARD_URL` must be the dashboard origin (`http://localhost:5180` locally): it is the CORS origin and a BetterAuth trusted origin.
