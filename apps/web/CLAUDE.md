# @buzzkit/web — the dashboard

Vite + React Router 8 SSR on Cloudflare Workers (`@cloudflare/vite-plugin`), deliberately not Next.js. Dev port **5180**, talks to the API on 8790. `docs/dashboard.md` is the route map, auth architecture, onboarding spec and phase plan; `docs/design.md` is the source of truth for everything visual (served at `/design.md`, previewed at `/ui`; both routes are development only and 404 in every other environment).

## Structure

```
app/
  root.tsx                      Layout, providers (Theme, `LinkProvider` handing react-router's `Link` to the design system, MotionConfig,
                                Tooltip, Toaster), ErrorBoundary
  cloudflare.ts                 `cloudflareContext`: env/ctx via RouterContextProvider
  routes.ts                     Route table (file-based, mirrors the API convention; brackets for params)
  lib/                          api.server.ts (typed Eden client; no parseDate — payloads keep dates as ISO strings so responses survive React Router's loader serialization without type/runtime drift) · session.server.ts (cookie, sign-out) · auth.client.ts (BetterAuth client)
                                actions/   every route action (`*.server.ts`), one file per feature, `context.server.ts` for the preamble
                                utils/     pure helpers (format, time, request: `requestUrl` is the only way to read a loader's URL, because
                                           React Router's single-fetch requests arrive as `/path.data?_routes=…` and a link or redirect built
                                           from the raw `request.url` would point at the data endpoint; json: `parseJson` with a line and column for every path and `lineOf`, behind the segment and workflow editors; pagination: `readPage` turns the request into `{ limit, cursor }`,
                                           `paginate` turns an API page into `{ items, pagination }` with page number, page count and
                                           Previous / Next hrefs, carrying the cursor trail in the URL)
                                `.server.ts` / `.client.ts` are React Router module boundaries: a `.server` file is excluded from the
                                browser bundle and the build fails if client code imports it, so Worker env and tokens cannot leak
  hooks/                        Client hooks
  components/<feature>/         Only pieces used by more than one route live here, one directory per feature, files named for what they
                                are (never prefixed, never abbreviated): conditions/ (the expression vocabulary every feature reads: describe.ts turns an expression into parts and trees, chips.tsx renders them as chips; segments, workflow triggers and message targets all use it) · badges/ (the vocabulary badges: one component per value set, owning label
                                and color, so a platform or key kind looks the same on every page) · events/ (describe.ts, the audit-log vocabulary; stream.ts, the event-stream vocabulary: label,
                                icon and detail per `$` name, custom names as themselves; name.tsx, the one-line glyph + label cell of the catalog and the stream; volume-chart.tsx, shared by the Events pages) · webhooks/ (describe.ts, the subscription summary shared by the list and the endpoint page) · sources/ (describe.ts: provider labels, the verification sentence, drop reasons and secret hints shared by the sources list and the source page) · workflows/ (describe.ts: the trigger, step and run-event vocabulary shared by the list, the workflow page and the run page; spec-editor.tsx: the JSON definition textarea with the `@buzzkit/schema/workflows` lint inline, used by New workflow and the Code tab; flow.tsx: the definition as a flow diagram (rules rows, boundary-centered node columns, forked lanes that continue or end the run) with an optional run path drawn along it, used by the Steps tab, the version dialog, the New workflow preview and the run page; trigger.tsx: the trigger (an event with its sources, or a schedule read as a sentence with its segment) and the cancel rules as segment-style condition chips, history conditions included; live-runs.tsx: the three live-run counts as dots with a tooltip, on the workflows list and the Overview) · overview/ (charts.tsx: the tiles, delta, period chart, line definitions and their skeletons the workspace Overview and the admin Overview both render) · auth/ (form, providers, password input) · command/ (menu.tsx, the ⌘K command menu the workspace layout renders once: page actions from `hooks/use-commands.ts`, id jumps, recents, every sidebar destination with its `G` chord, workspace and tenant switching, search fallbacks, help; page-commands.tsx, `PageCommands`, the component form of `useRegisterCommands` for inside a `Deferred` render prop; the pure parsing and catalog live in `lib/command.ts`, the live entity search is the loader-only route `routes/[slug]/search/index.ts`; `docs/dashboard.md`, Command menu) · layout/ (the signed-in chrome: sidebar + navigation.ts, the IA; brand-page.tsx is `BrandPage`, the signed-out frame: the brand wash and the notification cards from the marketing hero around the centered card, shared by login, signup, the invite page and the full-page errors;
                                account menu, workspace switcher, theme provider) · onboarding/ (layout, transition, progress, catalog,
                                choice-row, file-drop, connected, provider-guide; guides/ data; illustration/ primitives) · settings/ ·
                                workspace/ (fields, create dialog) · errors/ (frame.tsx is `ErrorFrame`, a `BrandPage` around a card; unexpected, no-access and not-found each export a full-page component for the root ErrorBoundary and a `*Notice` `EmptyState` for the same failure inside the workspace chrome). Anything a single route uses is written in that route file
  test/                         Unit tests mirroring `app/` for pure modules (`lib/utils/time`, `components/workflows/describe`), run with `bun test`
  routes/<segment>/index.tsx    Loader + `export const action = …Action` from lib/actions.server.ts + composition; pathless groups are
                                `(name)/` like Next.js (`(auth)/layout.tsx` wraps login and signup)
```

## Rules (non-negotiable)

- **The browser never holds the API token.** BetterAuth lives on the API (`/v1/auth`) and the browser signs in against it directly through `lib/auth.client.ts`; the httpOnly session cookie the API sets is same-site with the dashboard, so loaders read it and pass it on as the bearer token. Every `/v1/*` call happens in a loader or action through `lib/api.server.ts` (Eden over `@buzzkit/api/contract`, bearer = the session cookie's value, `buzzkit-workspace` / `buzzkit-tenant` headers). Any 401 expires the cookie and redirects to `/login`. Entity types are derived from the client (`Awaited<ReturnType<typeof listX>>`), never written by hand.
- **Navigation never waits on the API.** A loader awaits only local work (`requireSession`, `resolveTenant`, anything read off the URL) and returns its API calls as promises, wrapping a fetch-and-derive block in a single async IIFE when it needs to compute over the result. The route paints its chrome at once (title, description, action buttons, tab pills, filter bars) and hands the promise to `Deferred` (`components/loading/deferred.tsx`), which renders `children(data, pending)`: `data` is the resolved value, or what this pathname resolved to last time while a refresh is in flight, or `undefined` on a cold load. So a page shows a skeleton only the first time and keeps its rows on a return visit, a filter change or a page step. Skeletons come from `components/loading/` (`TableSkeleton`, `CardSkeleton`, `BlockSkeleton`); a table declares its columns once as `TableColumn[]` and renders them through `TableColumns` so the skeleton and the real header cannot drift. Every route renders its header through `PageHeader` (`components/layout/page-header.tsx`) in a `<Page>Header` component and its cold-load body in a `<Page>Skeleton`, and exports both inline as `export const handle: PageHandle = { skeleton: (<div className='flex min-h-0 w-full flex-1 flex-col gap-5'><XHeader canManage={null} /><XSkeleton /></div>) }` (`components/loading/handle.ts`; never through an intermediate const), which the workspace layout paints from `useMatches()` before the page can mount; the handle and the `Deferred` fallback must be the same pixels. A skeleton stands only for what is loading: the title, description, filters, time range and every button all roles get are real from the first frame, a role-gated button is the real button, `disabled` until the role is known and gone once the role says no (the same rule as filters on a cold load), anything the role decides resolves through `useCanManage` / `useKnownRole` (`hooks/use-known-role.tsx`, fed by the layout from the workspace list it already has or the `buzzkit.roles` cookie hint its loader writes on the first visit to a workspace, so only that first visit ever waits on the workspace list), an input, textarea or button that is loading is `InputSkeleton` / `TextareaSkeleton` (`components/loading/field.tsx`) or `ButtonSkeleton` (`components/loading/button.tsx`), which render the real control invisibly under a `Skeleton` overlay so the size is exact by construction, never a hand-sized rectangle (a `h-8.5` guess drifts the moment a control's height, padding or row count changes: the workflow description textarea was 4px short and the spec editor 200px tall) and never a framed empty input; a code block that is loading wraps an invisible placeholder of the same length in the block's own padding and type for the same reason; an inline skeleton inside a text line (a title, a description) is a `span` (a `div` inside a `p` is invalid HTML and a hydration error) and stays shorter than the line box (`h-4` in a base description) so it never grows the line, and a skeleton that stands in for the whole title of a flex `h1` (which has no line box to keep) is `h-[1.15em]`, the heading's `leading-tighter`, so the header keeps its height, and nothing ever shows a spinner in place of a page. Filter controls are disabled on a cold load (`data === undefined`), including a time range in the page header (Overview's `FilterRange`, an event's `PillTabs`); a header sits outside `Deferred`, so it asks `usePageCold()` (same file), which is true until this pathname has resolved once, instead of relaying state up from the body. Header action buttons stay enabled, they do not wait on the page's data. Each filter is disabled again while the change *it* made is loading: picking a value swaps the control to that value at once, spins in place of its chevron, and refuses a second pick until the rows land, while every other filter stays usable. React Router commits a navigation inside a transition, so while the rows load the committed tree still shows the *previous* navigation; `useFilters` therefore keeps its own record of where the page is heading (the composed search string, written synchronously on every pick and valid until the committed location key changes with no navigation in flight), reads values and `pending` from it, and composes the next pick on top of it, so a second click cannot drop the first and both keys spin until the rows land. `filters.pending.<key>` drives that spinner, and disabled means 50% opacity here as everywhere. A clear is the exception: the facets all snap back to "Any …" and go disabled (`disabled={cold || filters.clearing}`, dimmed, no spinner), while only Clear spins, mounted through the load until the rows land. `filters.searching` covers the query alone, so neither a facet nor a clear spins the search field. A `PillTabs` strip that drives a loader (the Events time range) takes the same `loading`, but without a spinner: the pill moves at once and the whole strip goes disabled and dimmed, because the strip is one picker. Search inputs take `loading` only, never `disabled`, because disabling steals focus mid-typing. `meta` cannot read deferred data, so entity pages carry a static tab title. A streamed promise has until `streamTimeout` (`entry.server.tsx`, 30s; React Router's default is 4950ms) to settle, after which the framework aborts it and the page's error boundary shows “Unexpected Server Error” with nothing in the server log; the same message appears when the promise rejects, so `lib/api.server.ts` logs every API failure it turns into an `ApiError` (`api failure` in Workers Logs) and that log line is the place to look first.
- **Loaders are the source of truth.** No client caches, no client fetching on a timer. **The dashboard is live by default**, and live always means *the route's own loader runs again*. The workspace layout calls `useLive()` (`hooks/use-live.ts`) once, for whatever page is mounted, so every page under `/:slug` refreshes every five seconds without asking for it: a new subscriber, a message moving through its deliveries, a run waking up or a teammate's audit entry appears without a reload. A page opts *out* through the handle it already exports — `export const handle: PageHandle = { skeleton: …, live: false }` — and that is the only switch; there is no per-page hook call to remember or forget. Opting out is for pages nothing outside this browser can change (Topics, Segments and their editors, workflow secrets, New workflow, the workflow test page, the settings forms) and for the event Stream, which keeps its own Tinybird polling and would have its accumulated rows reset by a loader refresh. The hook stands down while the tab is hidden (and revalidates the moment it comes back), and while a navigation, a revalidation or any fetcher is in flight, because `revalidate()` aborts in-flight fetchers; because `idle` is one of its effect dependencies it re-arms only after the last refresh settles, so refreshes never overlap or queue. The workspace layout's own `shouldRevalidate` keeps the chrome out of it, so only the leaf loader re-runs. `Deferred` keeps the rows on screen across a refresh and React Router applies the update in a transition, so no skeleton ever appears on a refresh, nothing flashes, and an open dialog keeps its focus and its typed text. Two exceptions to the loader rule remain, by design. The admin Overview's throughput strip polls the loader-only route `routes/admin/rates/index.ts` through a fetcher every ten seconds, because the page's own loader is several Tinybird queries and must not re-run on a timer. And the event Stream's first page queries Tinybird directly from the browser with the short-lived tenant-scoped JWT the loader fetched from `GET /v1/events/token`, never the API (`docs/engine.md`). A component whose state is seeded from loader data must not resync on loader *identity* — depend on the values it reads (`[open, workflow.name, workflow.description]`), never the object, or a refresh wipes what the user typed. Mutations are route actions with form intents (`useActionFetcher` for toast-style results, `useFetcher` when the result renders inline), and React Router revalidates. No action-only routes: the action lives on the route that renders the control, and its body lives in `lib/actions/<feature>.server.ts`, never in the route file.
- **Every page action is a command, and header actions register themselves.** A `Button` inside `PageHeader`'s `actions` slot becomes a command automatically (the design system's `ActionRegistryProvider`, wired by `PageHeader` to `hooks/use-commands.ts`): its text is the label, its `icon` the icon, and selecting it clicks the real button, so list routes register nothing. Only an action that lives outside the page header (a dropdown item, a card button, a copy of an id on a detail page) is registered by hand with `useRegisterCommands([{ id, label, icon, keywords, run | to }])`, under the same words as its control and only when the role allows it. Register only real actions: something that opens a dialog, mutates, or goes to a page no registered control already reaches. Never hand-register a filter value, a time range, a tab or any other view state as a command; the menu must learn those from the controls themselves, or not at all. Filters need nothing: every `FilterSelect` and `FilterRange` registers itself through the design system's `FilterRegistryProvider`, which the workspace layout wires to the same store, so the menu offers "Filter by status" and its options on any page that renders the bar. Never make the menu read route state any other way.
- **Design system only.** Components from `@buzzkit/ui/components/*`, tokens only (`bg-bg-2`, `text-fg-2`, `primary-*`), `<Icon name='Icon…' />` with string literals only (the icon generator scans for them), the press pattern, `text-balance` titles / `text-pretty` descriptions, sentence case, no em or en dashes in user-facing copy. New UI belongs on `/ui`.
- **Every page works on a phone, from the same tree.** No mobile variants, no hidden columns, no viewport checks in route code: `lg` folds the chrome (sidebar → sheet behind the top bar, document scroll inside the panel; `docs/dashboard.md` Layout) and `sm` folds rows (`PageHeader` and detail headers stack, `FilterBar` stacks, `CardHeader` wraps its action, `SettingsRow` and builder rows wrap; `docs/design.md` §13 "Small screens"). A fixed width needs a phone partner (`w-full sm:w-64`, `w-40 sm:w-48`), a tile grid is `grid-cols-2` at the bottom, `table-fixed` never applies under `lg` (tables also carry a horizontal scroll fade), and a card that is a sentence beside a button is `flex-col items-start sm:flex-row sm:items-center`. Check a new page at 390px before calling it done.
- **Every time is hoverable.** Render times only through `TimeAgo` (relative) or `Time` (date) from `hooks/use-time-ago.tsx`; both show the exact time in a tooltip. Never put `formatDate()` output straight into JSX.
- **Motion stack.** CSS transitions for state; `motion/react` for anything more complex (springs `bounce: 0`, `AnimatePresence initial={false}`); `NumberFlow` from `@buzzkit/ui/components/number-flow` for every changing number (never the raw `@number-flow/react`: the wrapper carries the one 400ms ease-out timing); `@buzzkit/ui/components/text-swap` `TextSwap` for short text that changes in place. No other animation libraries, no hand-rolled equivalents.
- **SSR is the Worker, and the Worker looks like a browser.** The Cloudflare environment resolves packages with the `browser` export condition, so anything that asks `esm-env` whether it is in a browser gets `true` during SSR and skips its server markup (NumberFlow rendered an empty element until the client upgraded it). `vite.config.ts` answers `esm-env/browser` with `false` in the `ssr` environment and keeps those packages out of the Worker's dep pre-bundle so the override applies; if a component is missing on first paint but fine after hydration, check that first. `NumberFlow` also ships an unlayered `:where(number-flow-react){line-height:1}` in its SSR markup that beats any layered `leading-*` utility until hydration removes it, so a number that is not `leading-none` grows on the second paint and shifts the layout: give `NumberFlow` `leading-none` and size the row around it.
- **Copy goes through the `copy` skill** (`.claude/skills/copy/SKILL.md`): one job per string, full sentences, the API's nouns, no reassurance or fragments. Page descriptions say what the page is for and stop; field hints say what the option is and where it is used; CTAs name the action ("Create key").
- **Component bodies read top to bottom in one fixed order.** (1) router and context hooks and custom hooks whose result the body uses (`useOutletContext`, `useNavigate`, `useSearchParams`, `useFetcher` / `useActionFetcher`, `useFilters`); (2) plain values derived from props and loader data; (3) every `useState`; (4) every `useRef`; (5) derived values; (6) custom hooks that return nothing (`useFocusFirstError`, `useLinkedScroll`), directly under the derived values; (7) handlers; (8) every `useEffect`, last, directly above the `return`. No unused imports, variables or props; nothing computed that the JSX does not use; no state that only mirrors a prop when a `key` or a derived value does the job. Modules: imports, then constants, then types, then helpers, then components, the route's default export last.
- **No comments in apps/web.** Naming, small modules and `docs/` explain; only `TEMPORARY` markers and lint directives are allowed.
- **Feature components are plain, and only exist when shared.** A component used by one route lives in that route file; `components/` is for pieces two or more routes use; reusable-beyond-the-app pieces (`GuideStep`, the logo as `IconBuzzkit`) go to `@buzzkit/ui`. No loaders or route entries inside `components/`.
- **Onboarding guides are data.** Add a provider by writing `components/onboarding/guides/<provider>.tsx` (steps + fields + illustrations built from `illustration/` primitives), registering it in `guides/index.ts` and the catalog, and extending `guides/upload.ts`. Illustrations follow the real third-party screen's structure and copy, drawn in our tokens with one `Spot` ring; never screenshots.

## Commands

| Command | Description |
|---|---|
| `bun dev` | Dev server on 5180 (needs `.dev.vars` with `API_URL`, see `.dev.vars.example`, and the API on 8790) |
| `bun check-types` | `wrangler types` + `react-router typegen` + `tsc` |
| `bun test` | vitest over `test/` (pure modules only: `lib/utils`, the `describe` vocabularies; nothing that renders or touches the network; `@/app` resolves through `vitest.config.ts`) |
| `bun build` / `bun deploy` | Build / deploy the Worker |
